---
phase: 17-direct-read-search-workflows
plan: 04
type: execute
wave: 4
depends_on:
  - 03
files_modified:
  - src/mcp_telegram/cache.py
  - src/mcp_telegram/tools.py
  - tests/test_cache.py
  - tests/test_tools.py
autonomous: true
gap_closure: true
requirements:
  - FLOW-01
  - FLOW-02

must_haves:
  truths:
    - "Common direct reads, including forum-topic reads and recovery semantics, remain available even when two independent MCP sessions hit the runtime concurrently"
    - "Common direct searches still preserve the Phase 17 numeric-dialog fast path and hit-local context after the cache hardening"
    - "Shared SQLite-backed cache startup no longer fails with `database is locked` during normal parallel MCP session startup"
    - "The concurrency hardening is proven both in automated coverage and against the restarted long-lived runtime"
  artifacts:
    - path: "src/mcp_telegram/cache.py"
      provides: "Concurrency-safe shared-cache bootstrap and maintenance boundaries"
      exports: ["Read-safe cache open path", "One-time or guarded initialization behavior", "No hot-path `PRAGMA optimize` lock hazard"]
    - path: "src/mcp_telegram/tools.py"
      provides: "Runtime entry behavior that no longer reintroduces unsafe per-process cache startup assumptions"
      exports: ["Shared cache acquisition path", "Unchanged direct read/search public contract"]
    - path: "tests/test_cache.py"
      provides: "Regression proof for constructor-time lock handling and concurrent cache bootstrap behavior"
      exports: ["Locked-init tolerance", "Concurrent startup expectations", "No regression of existing cache invariants"]
    - path: "tests/test_tools.py"
      provides: "Workflow-level proof that read and search flows still work after cache bootstrap hardening"
      exports: ["Direct forum-read regression coverage", "Direct search regression coverage", "No behavioral regression at tool level"]
  key_links:
    - from: "17-UAT.md"
      to: "src/mcp_telegram/cache.py"
      via: "closes the diagnosed parallel-session SQLite lock gap found during live runtime verification"
      pattern: "database is locked|parallel|EntityCache|optimize"
    - from: ".planning/debug/concurrent-cache-init-lock.md"
      to: "src/mcp_telegram/cache.py"
      via: "moves write-capable bootstrap work out of the hot constructor or guards it for cross-process safety"
      pattern: "journal_mode|optimize|DDL|concurrent"
    - from: "src/mcp_telegram/cache.py"
      to: "tests/test_cache.py"
      via: "proves cache bootstrap no longer fails under the diagnosed lock interleaving"
      pattern: "locked|concurrent|init|cache"
    - from: "17-UAT.md"
      to: "tests/test_tools.py"
      via: "preserves the direct forum-read and direct search behaviors that were already green in UAT while fixing the runtime gap"
      pattern: "ListMessages|SearchMessages|topic|search|direct"
---

<objective>
Close the Phase 17 runtime verification gap where parallel MCP sessions can fail during shared
SQLite cache initialization with `sqlite3.OperationalError: database is locked`.

Purpose: keep the direct read/search workflows reliable under normal multi-session runtime usage
without reopening the public tool contract work completed in Plans 01-03.
Output: concurrency-safe cache bootstrap, regression coverage, and restarted-runtime proof that
parallel MCP sessions can perform read-oriented work without constructor-time SQLite lock failures.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-direct-read-search-workflows/17-UAT.md
@.planning/debug/concurrent-cache-init-lock.md
@.planning/phases/17-direct-read-search-workflows/17-03-PLAN.md
@src/mcp_telegram/cache.py
@src/mcp_telegram/tools.py
@src/mcp_telegram/capabilities.py
@tests/test_cache.py
@tests/test_tools.py
@devtools/mcp_client/README.md
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make shared cache initialization read-safe across parallel MCP processes</name>
  <files>src/mcp_telegram/cache.py, src/mcp_telegram/tools.py</files>
  <action>
Harden the SQLite-backed cache bootstrap path so two independent MCP stdio sessions can start
against the same state directory without tripping constructor-time lock failures.

Required scope:
- isolate one-time schema/bootstrap work from the hot constructor path, or guard it so concurrent
  processes do not fail with `database is locked`
- remove or relocate `PRAGMA optimize` from normal per-process cache open if it can contend with
  active readers/writers
- preserve current cache schema, TTL behavior, and read/search tool semantics
- keep the fix bounded to the cache/runtime boundary instead of reopening public tool contracts

Guardrails:
- do not broaden this into an analytics redesign unless the same concrete lock path is proven there
- do not weaken direct read/search functionality as a workaround
- do not rely on "retry later" messaging as the primary fix if the startup path can be made safe
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache.py -k "locked or cross_process or concurrent" -q</automated>
  </verify>
  <done>Parallel MCP processes no longer fail in the shared cache bootstrap path under the diagnosed lock mechanism.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression proof for the diagnosed lock interleaving without regressing read/search flows</name>
  <files>tests/test_cache.py, tests/test_tools.py</files>
  <action>
Add coverage that proves the Phase 17 gap is actually closed and that the hardening does not
damage the direct read/search workflows that Phase 17 introduced.

Required coverage:
- cache/bootstrap regression that would have caught the constructor-time lock hazard
- direct `ListMessages` regression proving known-target forum-topic reads, recovery semantics, and
  navigation still function after the cache bootstrap changes
- direct `SearchMessages` regression proving numeric-dialog fast-path searches still return
  hit-local grouped output after the cache bootstrap changes
- if full deterministic parallel-process reproduction is too brittle for CI, add the strongest
  stable approximation in tests and reserve exact multi-session proof for runtime verification
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache.py -k "locked or cross_process or concurrent" -q && uv run pytest tests/test_tools.py -k "(list_messages and (direct or topic or navigation)) or (search_messages and (hit or context or navigation)) or routes_numeric_dialog_to_exact_capability" -q</automated>
  </verify>
  <done>The cache hardening is covered by regression tests and direct read/search behavior still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild and verify the long-lived runtime with parallel MCP sessions</name>
  <files>src/mcp_telegram/cache.py, src/mcp_telegram/tools.py</files>
  <action>
Because the gap was found only in live runtime verification, close it only after rebuilding and
retesting the long-lived container on this machine.

Required proof:
- rebuild and restart `mcp-telegram` using the documented compose file
- run two independent MCP client sessions against the restarted container using the same parallel
  reproduction shape that triggered the UAT gap
- verify concurrent read-oriented calls no longer fail with constructor-time SQLite lock errors in
  either session transcript
- confirm the existing direct read/search schema surface still matches the Phase 17 contract
  </action>
  <verify>
    <automated>docker compose -f /opt/docker/mcp-telegram/docker-compose.yml up -d --build mcp-telegram && rm -f /tmp/phase17-gap-a.log /tmp/phase17-gap-b.log && { (UV_CACHE_DIR=/tmp/.uv-cache uv run python -m devtools.mcp_client.cli call-tool --name ListMessages --arguments '{"exact_dialog_id":-1003779402801,"limit":3}' -- docker exec -i mcp-telegram mcp-telegram run > /tmp/phase17-gap-a.log 2>&1) & pid_a=$!; (UV_CACHE_DIR=/tmp/.uv-cache uv run python -m devtools.mcp_client.cli call-tool --name ListMessages --arguments '{"exact_dialog_id":-1003779402801,"exact_topic_id":6,"limit":3}' -- docker exec -i mcp-telegram mcp-telegram run > /tmp/phase17-gap-b.log 2>&1) & pid_b=$!; wait "$pid_a"; wait "$pid_b"; } && ! rg -n "database is locked|runtime execution failed" /tmp/phase17-gap-a.log /tmp/phase17-gap-b.log && UV_CACHE_DIR=/tmp/.uv-cache uv run python -m devtools.mcp_client.cli call-tool --name SearchMessages --arguments '{"dialog":"-1003779402801","query":"MCP","limit":2}' -- docker exec -i mcp-telegram mcp-telegram run && docker exec mcp-telegram /opt/venv/bin/python -c "from mcp_telegram.tools import ListMessages, SearchMessages, tool_description; list_schema = tool_description(ListMessages).inputSchema['properties']; search_schema = tool_description(SearchMessages).inputSchema['properties']; assert 'exact_dialog_id' in list_schema; assert 'exact_topic_id' in list_schema; assert 'dialog' in search_schema and 'query' in search_schema and 'navigation' in search_schema; assert 'exact_dialog_id' not in search_schema; print('schema_assertions_passed')"</automated>
  </verify>
  <done>The restarted runtime no longer exhibits the diagnosed parallel-session lock failure and still exposes the intended Phase 17 tool surface.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. the shared cache constructor path no longer performs lock-prone maintenance on every process start
2. direct forum-topic reads and direct numeric-dialog searches still behave as Phase 17 intended after the cache hardening
3. automated coverage would catch a regression in the diagnosed lock mechanism
4. the restarted runtime survives the original parallel MCP session reproduction without `database is locked`
</verification>

<success_criteria>
- [ ] The diagnosed UAT gap can be marked resolved after execution
- [ ] Parallel MCP sessions can start read-oriented work without constructor-time SQLite lock failures
- [ ] Direct read/search workflows remain intact after the cache hardening
- [ ] The long-lived runtime is rebuilt and manually rechecked for the original failure mode
- [ ] No unnecessary tool-surface or analytics redesign is introduced while closing this gap
</success_criteria>

<output>
After completion, create `.planning/phases/17-direct-read-search-workflows/17-04-SUMMARY.md` with:
- the diagnosed SQLite cache startup lock root cause addressed
- regression coverage added for the cache bootstrap hazard
- restarted-runtime parallel session verification performed
- direct read/search contract preserved while closing the gap
</output>
