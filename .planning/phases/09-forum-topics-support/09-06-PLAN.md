---
phase: 09-forum-topics-support
plan: 06
type: execute
mode: gap_closure
wave: 6
depends_on: [09-04, 09-05]
files_modified:
  - cli.py
  - tests/test_cli.py
  - .planning/phases/09-forum-topics-support/09-MANUAL-VALIDATION.md
autonomous: true
requirements:
  - TOPIC-01
  - TOPIC-02

must_haves:
  truths:
    - "There is a concrete operator path to inspect live topic catalogs, per-topic anchors, and by-ID refresh results without adding a new MCP tool"
    - "Phase 9 has an exact runtime validation procedure for 100+ topic pagination, deleted/private topics, and post-fix topic+unread behavior"
    - "Live validation closes against the rebuilt container runtime, not just the local checkout"
  artifacts:
    - path: "cli.py"
      provides: "Read-only debug commands for topic catalog inspection and topic-by-ID refresh in the live runtime"
      exports:
        - "debug-topic-catalog"
        - "debug-topic-by-id"
    - path: "tests/test_cli.py"
      provides: "Smoke tests for the new topic debug CLI output and argument wiring"
      exports:
        - "test_cli_debug_topic_catalog"
        - "test_cli_debug_topic_by_id"
    - path: ".planning/phases/09-forum-topics-support/09-MANUAL-VALIDATION.md"
      provides: "Exact live-validation checklist and commands for the rebuilt container runtime"
  key_links:
    - from: "cli.py debug-topic-catalog"
      to: "_fetch_forum_topics_page/_load_dialog_topics"
      via: "operator can inspect page boundaries, topic IDs, titles, deleted flags, and top_message_id values in the same runtime that serves MCP"
      pattern: "debug-topic-catalog"
    - from: "cli.py debug-topic-by-id"
      to: "_refresh_topic_by_id"
      via: "operator can revalidate one resolved topic and compare cached vs refreshed metadata during live debugging"
      pattern: "debug-topic-by-id"
    - from: "09-MANUAL-VALIDATION.md"
      to: "docker compose runtime restart and container verification"
      via: "manual validation uses the rebuilt `mcp-telegram` container, not stale local code"
      pattern: "docker compose -f /opt/docker/mcp-telegram/docker-compose.yml up -d --build mcp-telegram"
---

<objective>
Create the remaining credible path to roadmap live-validation closure after the code gaps are fixed.

Purpose: let the executor prove Phase 9 against a real forum with 100+ topics, deleted/private cases, and the updated topic+unread behavior in the actual runtime container.
Output: operator-facing topic debug commands plus a rebuilt-runtime validation playbook.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-forum-topics-support/09-VERIFICATION.md
@.planning/phases/09-forum-topics-support/09-MANUAL-VALIDATION.md
@.planning/phases/09-forum-topics-support/09-04-PLAN.md
@.planning/phases/09-forum-topics-support/09-05-PLAN.md
@cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add smoke tests for live topic-debug CLI commands</name>
  <files>tests/test_cli.py, cli.py</files>
  <action>
Create lightweight CLI smoke tests before implementation so the debug surface is intentional and stable.

Required tests:
- `test_cli_debug_topic_catalog` — the command accepts a dialog identifier, prints page-aware topic rows, and includes topic ID, title, top message anchor, and deleted/general flags.
- `test_cli_debug_topic_by_id` — the command accepts a dialog identifier plus topic ID, performs a by-ID refresh path, and prints both cached and refreshed metadata clearly enough for live debugging.

These are read-only diagnostics. Keep them separate from the MCP tool surface and mock Telegram access in tests so the suite stays fast.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -k "debug_topic" -v</automated>
  </verify>
  <done>The CLI debug entrypoints are test-defined before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement read-only topic debug commands in cli.py</name>
  <files>cli.py</files>
  <action>
Extend the local debug CLI with exactly the commands needed to close the live-validation gap.

Implementation targets:
- `debug-topic-catalog` — resolve the dialog, page through topic metadata, and print stable rows showing topic ID, title, `top_message_id`, deleted/general state, and page/offset progression.
- `debug-topic-by-id` — resolve the dialog, inspect cached topic metadata, run `_refresh_topic_by_id(...)`, and print the before/after values.
- Reuse existing internal helpers instead of duplicating Telegram logic.
- Keep the commands read-only and operator-focused; do not expose them as new MCP tools.

The operator needs enough visibility to answer two questions quickly in the live runtime:
1. Is the topic catalog/anchor data itself correct?
2. Does a failing topic become deleted, re-anchored, or remain inaccessible after by-ID refresh?
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -k "debug_topic" -v</automated>
  </verify>
  <done>The repo has a read-only debug surface for live forum topic inspection without widening the MCP API.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the manual validation playbook around rebuilt-runtime closure</name>
  <files>.planning/phases/09-forum-topics-support/09-MANUAL-VALIDATION.md</files>
  <action>
Update the manual validation doc so it becomes the final closure checklist for Phase 9 instead of a broad exploratory note.

The updated playbook must include:
1. Runtime rebuild and restart commands for `/opt/docker/mcp-telegram/docker-compose.yml`
2. Verification commands run inside the container so the operator proves the live MCP runtime is current
3. Exact `cli.py` commands for:
   - catalog pagination past the first page
   - by-ID refresh for a previously failing topic
   - topic-thread fetches for first page, next cursor, `from_beginning=True`, and `unread=True`
4. Evidence to capture for the roadmap criterion:
   - forum with 100+ topics
   - deleted or inaccessible topic case
   - post-fix topic+unread behavior
   - no adjacent-topic leakage

Keep the playbook concrete enough that another operator can execute it verbatim.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-forum-topics-support/09-MANUAL-VALIDATION.md</automated>
  </verify>
  <done>Phase 9 has a concrete, rebuilt-runtime closure checklist for the remaining live validation work.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. `uv run pytest tests/test_cli.py -k "debug_topic" -v` passes
2. `uv run cli.py debug-topic-catalog --help` and `uv run cli.py debug-topic-by-id --help` both work
3. The manual validation doc includes rebuilt-runtime commands and exact CLI invocations
4. The operator can inspect a failing topic's cached vs refreshed metadata without changing the MCP surface
5. The plan leaves a concrete route to verify roadmap criterion 5 on a real 100+ topic forum
</verification>

<success_criteria>
- [ ] The executor has read-only live-debug commands for topic catalog and by-ID refresh inspection
- [ ] Manual validation is rewritten as a rebuilt-runtime closure checklist
- [ ] Live closure can distinguish stale metadata, deleted topics, and inaccessible topics in the running container
- [ ] The path to roadmap criterion 5 no longer depends on guesswork or name-only probing
- [ ] Phase 9 can be re-verified against the actual deployed runtime once 09-04 and 09-05 land
</success_criteria>

<output>
After completion, create `.planning/phases/09-forum-topics-support/09-06-SUMMARY.md` with:
- debug CLI command inventory
- rebuilt-runtime validation steps to execute
- evidence checklist for roadmap criterion 5
- any residual live-data blockers
</output>
