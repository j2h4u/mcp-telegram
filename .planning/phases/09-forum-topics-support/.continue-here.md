---
phase: 09-forum-topics-support
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-12T13:23:03.393Z
---

<current_state>
Paused during Phase 9, Wave 5, plan `09-05`.

`09-04` is fully closed:
- code/test commits landed
- `.planning/phases/09-forum-topics-support/09-04-SUMMARY.md` exists
- `.planning/STATE.md` and `.planning/ROADMAP.md` were updated
- live container was rebuilt and spot-checked

`09-05` is partially closed:
- all three code/test commits exist
- focused unread/topic regression suite is green locally
- `09-05` summary does not exist yet
- runtime rebuild/restart and in-container verification for `09-05` have not been done yet
- roadmap/state still show Phase 9 at `4/6`
</current_state>

<completed_work>

- Task 1: Add live-representative regressions for topic-scoped unread behavior - Done in commit `ab8d2f3`
- Task 2: Implement topic-aware unread fetch rules for General and non-General topics - Done in commit `0d0ab09`
- Task 3: Stabilize unread cursors and empty-page behavior under topic filtering - Code/tests committed in `b206c67`; runtime verification and plan closure still pending

- Local verification already run successfully:
  - `uv run pytest tests/test_tools.py -k "topic_unread or unread_filter" -v`
  - Passed: `test_list_messages_unread_filter`
  - Passed: `test_list_messages_topic_unread_behavior`
  - Passed: `test_list_messages_general_topic_unread_is_scoped`
  - Passed: `test_list_messages_topic_unread_cursor_is_topic_scoped`
  - Passed: `test_list_messages_topic_unread_filters_dialog_leaks`
  - Passed: `test_list_messages_topic_unread_empty_page_has_no_cursor`
</completed_work>

<remaining_work>

- Finish Task 3 closure for `09-05`:
  - rebuild runtime: `docker compose -f /opt/docker/mcp-telegram/docker-compose.yml up -d --build mcp-telegram`
  - verify inside container that deployed `list_messages` includes the unread topic-scoping path
  - create `.planning/phases/09-forum-topics-support/09-05-SUMMARY.md`
  - update `.planning/STATE.md` and `.planning/ROADMAP.md` to mark `09-05` complete
  - commit those docs as a separate `docs(09-05): ...` commit

- Execute `09-06` after `09-05` is closed:
  - add `debug-topic-catalog` and `debug-topic-by-id` to `cli.py`
  - add `tests/test_cli.py` coverage
  - rewrite `.planning/phases/09-forum-topics-support/09-MANUAL-VALIDATION.md`
  - rebuild runtime again and verify inside the container
</remaining_work>

<decisions_made>

- `09-04` decision: treat `TOPIC_ID_INVALID` as a one-shot recoverable topic-state mismatch before surfacing a user-visible failure. This split stale anchors from deleted/inaccessible topics.
- `09-05` decision: keep `read_inbox_max_id` as the unread lower bound, but let topic filtering decide which fetched messages count toward the returned page and cursor.
- `09-05` decision: General-topic unread mode must be explicitly filtered; absence of `reply_to` is not enough to prove a message belongs to General.
- `09-05` decision: topic unread cursors are derived only from emitted topic-scoped messages; empty filtered pages must not reuse a dialog-wide cursor.
</decisions_made>

<blockers>
- External blocker: user paused due to blackout.
- Operational blocker when resuming: runtime verification for `09-05` still needs Docker access and in-container inspection.
- Workspace note: several planning files and `AGENTS.md` are untracked in git already; do not accidentally add them when committing the handoff or remaining plan docs.
</blockers>

<context>
The main execution pattern is working, but both `09-04` and `09-05` executors stalled after landing useful commits and before finishing summary/state/runtime closure. For `09-04`, the workaround was to verify the commits manually, rebuild the container locally, inspect the deployed package via `docker exec`, write the summary/state/roadmap updates, and commit only those metadata files.

Do the same for `09-05` instead of rerunning the whole plan from scratch. The code is already there. The next fresh session should treat `09-05` as "implementation done, closure missing", not as an untouched plan.

After `09-05` is formally closed, continue directly into `09-06`. Phase 9 is not done yet; roadmap criterion 5 still needs the debug CLI path and rebuilt-runtime validation checklist.
</context>

<next_action>
Start with: verify the `09-05` commits are present, rebuild the `mcp-telegram` container, inspect the deployed unread topic-scoping path inside the running container, then write `09-05-SUMMARY.md` and update roadmap/state before touching `09-06`.
</next_action>
