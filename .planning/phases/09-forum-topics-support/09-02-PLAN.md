---
phase: 09-forum-topics-support
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/mcp_telegram/tools.py
  - tests/test_tools.py
  - tests/conftest.py
autonomous: true
requirements:
  - TOPIC-01
  - TOPIC-03

must_haves:
  truths:
    - "ListMessages accepts topic: str | None and resolves the topic inside the already-resolved dialog only"
    - "Non-General topics prefer server-side thread retrieval via iter_messages(..., reply_to=topic_root_id)"
    - "ListMessages output shows the resolved topic name when a topic filter is active"
    - "Cursor handling remains correct for topic-filtered pagination, including from_beginning mode"
  artifacts:
    - path: "src/mcp_telegram/tools.py"
      provides: "ListMessages topic argument, dialog-scoped topic resolution, topic-aware message fetch path, and topic header output"
      exports: ["ListMessages", "list_messages"]
    - path: "tests/test_tools.py"
      provides: "Topic-focused handler tests for resolution, header output, cursor paging, and reverse iteration"
      exports: ["test_list_messages_topic_resolves_within_dialog", "test_list_messages_topic_header", "test_list_messages_topic_cursor_round_trip", "test_list_messages_topic_from_beginning", "test_list_messages_topic_sender_behavior", "test_list_messages_topic_unread_behavior"]
    - path: "tests/conftest.py"
      provides: "Forum-topic fixtures/helpers for reply headers and topic-root message mocks"
      exports: ["make_mock_forum_reply", "make_mock_topic"]
  key_links:
    - from: "ListMessages.topic"
      to: "dialog-scoped topic catalog"
      via: "dialog resolves first, then resolve() runs against one dialog's topic choices"
      pattern: "args.topic"
    - from: "resolved topic metadata"
      to: "iter_messages"
      via: "non-General topic path uses reply_to=topic_root_id when supported"
      pattern: "reply_to="
    - from: "resolved topic name"
      to: "final tool output"
      via: "topic header prefix is prepended before formatted message body"
      pattern: "topic:"
---

<objective>
Integrate forum topic support into `ListMessages`: add the `topic` parameter, resolve it within one dialog, fetch topic-scoped messages, and show the active topic name in the response header.

Purpose: let the LLM ask for messages from a named forum topic without cross-dialog ambiguity or manual topic-ID lookup.
Output: topic-aware `ListMessages` behavior plus tests for topic resolution, topic header display, cursoring, and reverse pagination.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-forum-topics-support/09-RESEARCH.md
@.planning/phases/09-forum-topics-support/09-VALIDATION.md
@.planning/phases/09-forum-topics-support/09-01-PLAN.md
@src/mcp_telegram/tools.py
@src/mcp_telegram/formatter.py
@tests/conftest.py
@tests/test_tools.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add topic parameter and dialog-scoped topic resolution to ListMessages</name>
  <files>tests/test_tools.py, src/mcp_telegram/tools.py</files>
  <action>
Write tests first, then extend the tool arguments and resolution flow.

Test targets:
- `test_list_messages_topic_resolves_within_dialog` — same topic title in different dialogs does not create cross-dialog ambiguity because the dialog resolves first.
- `test_list_messages_topic_not_found` — missing topic returns a clear topic-specific error.
- `test_list_messages_topic_ambiguous_within_dialog` — fuzzy matches inside one dialog return candidates instead of silently auto-resolving.

Implementation targets:
- Add `topic: str | None = None` to `ListMessages`.
- After dialog resolution, load the dialog's topic catalog from Plan 01 helpers and run `resolve()` against that catalog only.
- Keep topic resolution logic local to `list_messages`; do not change `resolver.py` to understand dialog/topic tuples globally.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "topic_resolves_within_dialog or topic_not_found or topic_ambiguous_within_dialog" -v</automated>
  </verify>
  <done>`ListMessages` accepts `topic`, and topic name resolution is dialog-scoped with explicit not-found and ambiguity behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement topic-aware message retrieval and output header</name>
  <files>tests/test_tools.py, tests/conftest.py, src/mcp_telegram/tools.py</files>
  <action>
Write topic-fetch behavior tests before implementation, then wire the topic path into `list_messages`.

Test targets:
- `test_list_messages_topic_header` — active topic name is shown in output before the message body.
- `test_list_messages_topic_cursor_round_trip` — topic-filtered pagination returns a stable `next_cursor`.
- `test_list_messages_topic_from_beginning` — reverse iteration works for topic-filtered history.

Implementation targets:
- Prefer server-side thread retrieval with `client.iter_messages(..., reply_to=topic_root_id)` for non-General topics.
- Keep cursor behavior compatible with the existing cursor encoder/decoder.
- Preserve reply-map and reaction-name enrichment on the topic-filtered message set.
- For header output, prepend a small topic line in `list_messages` rather than expanding `formatter.py` unless that becomes necessary.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "topic_header or topic_cursor_round_trip or topic_from_beginning" -v</automated>
  </verify>
  <done>Topic-filtered `ListMessages` retrieves the intended thread, paginates correctly, and shows the active topic name in the response header.</done>
</task>

<task type="auto">
  <name>Task 3: Define and test interaction rules for sender and unread filters</name>
  <files>src/mcp_telegram/tools.py, tests/test_tools.py</files>
  <action>
Make the topic interactions with existing filters explicit.

Required decisions to encode in code and tests:
- `topic + sender`: if server-side `reply_to` and `from_user` cannot be combined reliably, filter by sender after topic retrieval and document that behavior in code comments/tests.
- `topic + unread`: preserve support if possible; otherwise return a clear, test-backed incompatibility message instead of silent misbehavior.

Name the tests explicitly:
- `test_list_messages_topic_sender_behavior`
- `test_list_messages_topic_unread_behavior`

Test at least one happy path and one edge path so the combined behavior is intentional, not accidental.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "topic_sender or topic_unread" -v</automated>
  </verify>
  <done>`ListMessages` has explicit, test-backed behavior for `topic + sender` and `topic + unread` instead of undefined flag interactions.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. `ListMessages` schema exposes `topic`
2. Topic resolution is scoped to the resolved dialog only
3. Topic header appears in output when filter is active
4. Cursor pagination and `from_beginning` continue to work under topic filtering
5. sender/unread combinations are explicit and tested
</verification>

<success_criteria>
- [ ] `ListMessages` accepts `topic: str | None`
- [ ] Topic resolution is dialog-scoped and ambiguity-safe
- [ ] Topic-filtered retrieval works for non-General topics
- [ ] Topic name appears in the output header
- [ ] Topic pagination works with cursor and `from_beginning`
</success_criteria>

<output>
After completion, create `.planning/phases/09-forum-topics-support/09-02-SUMMARY.md` with:
- final ListMessages topic flow
- header/output behavior
- cursor/from_beginning behavior
- sender/unread interaction decisions
</output>
