---
phase: 09-forum-topics-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_telegram/cache.py
  - src/mcp_telegram/tools.py
  - tests/test_cache.py
  - tests/test_tools.py
autonomous: true
requirements:
  - TOPIC-02

must_haves:
  truths:
    - "A dialog-scoped topic metadata cache exists in entity_cache.db and does not overload the entities table"
    - "Topic enumeration paginates through channels.GetForumTopicsRequest using offset_date, offset_id, and offset_topic"
    - "Deleted topics can be detected explicitly via GetForumTopicsByIDRequest returning ForumTopicDeleted"
    - "General topic handling is normalized explicitly and is not hard-coded as topic 0"
  artifacts:
    - path: "src/mcp_telegram/cache.py"
      provides: "TopicMetadataCache table/class with TTL-aware read/write helpers and deleted-topic markers"
      exports: ["TopicMetadataCache"]
    - path: "src/mcp_telegram/tools.py"
      provides: "Forum-topic fetch helpers that paginate, normalize General topic, and support by-ID refresh"
      exports: ["_fetch_forum_topics_page", "_load_dialog_topics", "_refresh_topic_by_id"]
    - path: "tests/test_cache.py"
      provides: "Cache tests covering topic metadata round-trip, TTL, and tombstones"
      exports: ["test_topic_metadata_cache_round_trip", "test_topic_metadata_cache_ttl", "test_topic_metadata_cache_deleted_marker"]
    - path: "tests/test_tools.py"
      provides: "Topic fetch helper tests for topic pagination and deleted-topic by-ID refresh"
      exports: ["test_fetch_forum_topics_paginates", "test_refresh_topic_by_id_detects_deleted"]
  key_links:
    - from: "channels.GetForumTopicsRequest"
      to: "TopicMetadataCache"
      via: "paginated topic fetch stores dialog-scoped topic rows with top_message_id and flags"
      pattern: "GetForumTopicsRequest"
    - from: "channels.GetForumTopicsByIDRequest"
      to: "deleted topic handling"
      via: "by-ID refresh detects ForumTopicDeleted and updates cache tombstone"
      pattern: "GetForumTopicsByIDRequest"
    - from: "TopicMetadataCache"
      to: "ListMessages topic resolution"
      via: "tools.py loads active topics for one dialog and builds a local choices mapping"
      pattern: "topic.*dialog_id"
---

<objective>
Build the topic metadata foundation for Phase 9: a short-TTL dialog-scoped cache plus Telethon helpers for paginated forum topic enumeration and deleted-topic refresh.

Purpose: give `ListMessages(topic=...)` a reliable, dialog-local topic catalog with enough metadata to resolve names, detect tombstones, and anchor thread retrieval.
Output: TopicMetadataCache in `cache.py`, Telethon topic fetch helpers in `tools.py`, and focused tests for cache TTL, pagination, and deleted-topic refresh.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-forum-topics-support/09-RESEARCH.md
@.planning/phases/09-forum-topics-support/09-VALIDATION.md
@src/mcp_telegram/cache.py
@src/mcp_telegram/tools.py
@tests/test_cache.py
@tests/test_tools.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TopicMetadataCache with TTL and tombstone support</name>
  <files>tests/test_cache.py, src/mcp_telegram/cache.py</files>
  <action>
Add topic-cache tests first, then implement the new cache table/class.

Test targets:
- `test_topic_metadata_cache_round_trip` — upsert active topics for one dialog and verify they can be read back as dialog-scoped metadata with `topic_id`, `title`, `top_message_id`, `is_general`, and `is_deleted`.
- `test_topic_metadata_cache_ttl` — monkeypatch time so stale rows are treated as expired and trigger cache miss behavior.
- `test_topic_metadata_cache_deleted_marker` — store a deleted topic tombstone and verify active-topic reads exclude it while by-ID lookup still sees the deleted marker.

Implementation targets in `cache.py`:
- Add a dedicated `topic_metadata` table in the same SQLite database as `entities` and `reaction_metadata`.
- Include columns: `dialog_id`, `topic_id`, `title`, `top_message_id`, `is_general`, `is_deleted`, `updated_at`.
- Add a `TopicMetadataCache` class with clear helpers such as `get_dialog_topics(...)`, `get_topic(...)`, and `upsert_topics(...)`.
- Keep the interface dialog-scoped; do not add topics to `EntityCache.all_names_with_ttl()`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache.py -k "topic_metadata_cache" -v</automated>
  </verify>
  <done>TopicMetadataCache exists with passing tests for round-trip storage, TTL expiry, and deleted-topic tombstones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add paginated topic fetch helpers using raw Telethon requests</name>
  <files>tests/test_tools.py, src/mcp_telegram/tools.py</files>
  <action>
Add helper-focused tests before implementation, then build the topic fetch layer in `tools.py`.

Test targets:
- `test_fetch_forum_topics_paginates` — mock two `GetForumTopicsRequest` pages and verify helper returns the full topic set without duplicates or off-by-one errors.
- `test_refresh_topic_by_id_detects_deleted` — mock `GetForumTopicsByIDRequest` returning `ForumTopicDeleted` and verify the helper updates/decorates cache state as deleted.

Implementation targets:
- Add small private helpers for one-page fetch, full pagination, and by-ID refresh.
- Use raw `channels.GetForumTopicsRequest` / `channels.GetForumTopicsByIDRequest`; do not invent a separate abstraction layer.
- Normalize the General topic explicitly. If Telegram/Telethon live behavior omits it from the topic list, synthesize a stable "General" row from the normalized rule instead of hard-coding roadmap text that says topic `0`.
- Persist fetched topic metadata into `TopicMetadataCache` with a 5-10 minute TTL boundary.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "forum_topics_paginates or refresh_topic_by_id_detects_deleted" -v</automated>
  </verify>
  <done>tools.py contains reliable topic-fetch helpers that page through forum topics, normalize General topic behavior, and detect deleted topics through a by-ID refresh path.</done>
</task>

<task type="auto">
  <name>Task 3: Wire cache-backed topic catalog loading for one resolved dialog</name>
  <files>src/mcp_telegram/tools.py, tests/test_tools.py</files>
  <action>
Add one higher-level helper that `ListMessages` can call later:

- given a resolved dialog ID, load fresh topic metadata from cache if within TTL
- otherwise fetch from Telegram, persist to cache, and return active topics plus any tombstones

The helper should return enough structure for downstream resolution:
- active topic choices `{topic_id: title}`
- a metadata map keyed by `topic_id`
- deleted-topic awareness for clearer error handling later

Keep this helper internal to `tools.py` for now unless the file gets unreasonably large. The current codebase is still small enough that a tiny internal helper is less overhead than creating a new module prematurely.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache.py tests/test_tools.py -k "topic" -v</automated>
  </verify>
  <done>There is a single cache-backed topic catalog loader ready for `ListMessages(topic=...)` to consume in the next plan.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. Topic metadata lives in a dedicated cache table, not in `entities`
2. Topic enumeration paginates beyond the first page with no duplicates or missed topics
3. Deleted topics are detected via by-ID refresh rather than inferred from empty message history
4. General topic normalization is explicit and test-backed
5. `uv run pytest tests/test_cache.py tests/test_tools.py -k "topic" -v` passes
</verification>

<success_criteria>
- [ ] `TopicMetadataCache` exists in `src/mcp_telegram/cache.py`
- [ ] Topic metadata table includes dialog scope, top message linkage, General/deleted flags, and TTL timestamp
- [ ] Raw Telethon helpers in `src/mcp_telegram/tools.py` page through topics and refresh by ID
- [ ] General topic is normalized without hard-coding topic `0`
- [ ] Deleted topic detection is test-backed and cache-aware
</success_criteria>

<output>
After completion, create `.planning/phases/09-forum-topics-support/09-01-SUMMARY.md` with:
- cache schema and helper API
- General topic normalization decision
- deleted-topic refresh behavior
- pagination test results
</output>
