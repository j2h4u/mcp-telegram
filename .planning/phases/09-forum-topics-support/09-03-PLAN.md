---
phase: 09-forum-topics-support
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - src/mcp_telegram/tools.py
  - tests/conftest.py
  - tests/test_tools.py
  - .planning/phases/09-forum-topics-support/09-MANUAL-VALIDATION.md
autonomous: true
requirements:
  - TOPIC-01
  - TOPIC-02
  - TOPIC-03

must_haves:
  truths:
    - "Deleted and inaccessible topics have explicit, user-visible behavior instead of silent fallback or silent omission"
    - "General topic behavior is validated against real Telegram semantics and not assumed from roadmap text"
    - "Phase 9 includes a live-forum validation playbook for 100+ topics and pagination boundaries"
    - "Topic filtering does not leak messages from adjacent topics across page boundaries"
  artifacts:
    - path: "src/mcp_telegram/tools.py"
      provides: "Final hardening for deleted/private topic behavior and any fallback path needed after tests"
      exports: ["list_messages"]
    - path: "tests/conftest.py"
      provides: "Richer forum-topic fixtures covering General, deleted, and inaccessible topic cases"
      exports: ["make_deleted_topic", "make_general_topic_message", "make_private_topic_error"]
    - path: "tests/test_tools.py"
      provides: "Regression tests for deleted/private/general topic cases and pagination boundaries"
      exports: ["test_list_messages_deleted_topic_behavior", "test_list_messages_general_topic_normalization", "test_list_messages_private_or_inaccessible_topic_behavior", "test_list_messages_topic_boundary_no_leakage"]
    - path: ".planning/phases/09-forum-topics-support/09-MANUAL-VALIDATION.md"
      provides: "Step-by-step live forum validation guide for 100+ topics, deleted topics, and reverse pagination"
  key_links:
    - from: "deleted/private topic fetch"
      to: "user-visible fallback"
      via: "tool emits explicit warning/error path and avoids pretending the filter succeeded"
      pattern: "TopicDeletedError|RPCError"
    - from: "General topic normalization"
      to: "live validation guide"
      via: "manual test confirms actual ID/root-message behavior before shipping"
      pattern: "General"
    - from: "topic page boundary tests"
      to: "no message leakage"
      via: "assert adjacent-topic messages never appear in filtered output"
      pattern: "boundary"
---

<objective>
Harden the forum-topic implementation around the real risks: General-topic normalization, deleted/private topics, pagination boundaries, and live validation in an actual forum supergroup.

Purpose: close the gap between mocked correctness and production-safe correctness for forum topics.
Output: regression tests for edge cases, any final `tools.py` hardening required by those tests, and a manual validation playbook for live Telegram verification.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-forum-topics-support/09-RESEARCH.md
@.planning/phases/09-forum-topics-support/09-VALIDATION.md
@.planning/phases/09-forum-topics-support/09-01-PLAN.md
@.planning/phases/09-forum-topics-support/09-02-PLAN.md
@src/mcp_telegram/tools.py
@tests/conftest.py
@tests/test_tools.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for General, deleted, and inaccessible topics</name>
  <files>tests/conftest.py, tests/test_tools.py, src/mcp_telegram/tools.py</files>
  <action>
Write failing tests for the edge cases called out in research, then harden the implementation until they pass.

Required tests:
- `test_list_messages_general_topic_normalization` — General topic resolves through the normalized rule rather than assuming roadmap text "topic 0".
- `test_list_messages_deleted_topic_behavior` — deleted topic produces an explicit message or warning path that reflects actual behavior.
- `test_list_messages_private_or_inaccessible_topic_behavior` — inaccessible topic path is explicit and does not pretend filtered success.

If these tests force a policy choice, prefer explicitness over silent fallback. The user should never think the topic filter worked if the implementation had to drop back to unfiltered history.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "general_topic_normalization or deleted_topic_behavior or private_or_inaccessible_topic_behavior" -v</automated>
  </verify>
  <done>General, deleted, and inaccessible topic behaviors are encoded in tests and supported by explicit tool behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock down pagination boundaries and no-leakage guarantees</name>
  <files>tests/test_tools.py, src/mcp_telegram/tools.py</files>
  <action>
Add regression coverage for the failure mode most likely to escape mocks: leaking messages across topic boundaries when paging.

Required tests:
- first page contains only target-topic messages
- next page contains only target-topic messages
- reverse pagination (`from_beginning=True`) contains only target-topic messages
- cursor boundaries do not duplicate or skip thread messages

If the preferred `reply_to=` path cannot guarantee this under one flag combination, harden the code with a fallback client-side filter before formatting output.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "topic_boundary_no_leakage or topic_cursor" -v</automated>
  </verify>
  <done>Topic filtering is boundary-safe: no adjacent-topic messages leak into paged results, and cursor transitions remain stable.</done>
</task>

<task type="auto">
  <name>Task 3: Write the live-forum manual validation playbook</name>
  <files>.planning/phases/09-forum-topics-support/09-MANUAL-VALIDATION.md</files>
  <action>
Create a concise manual validation doc for the executor/operator covering:

1. Forum setup assumptions (forum-enabled supergroup, 100+ topics if possible)
2. How to identify/validate the General topic behavior
3. How to validate topic metadata pagination beyond the first page
4. How to validate deleted/private topic behavior
5. How to validate topic-filtered `ListMessages` for:
   - first page
   - next cursor
   - `from_beginning=True`
   - sender/unread combinations if supported

Include exact commands to run during manual validation once implementation exists.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-forum-topics-support/09-MANUAL-VALIDATION.md</automated>
  </verify>
  <done>A live-forum validation playbook exists and is ready for execution once the code changes land.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. Edge-case topic behavior is explicit and test-backed
2. Topic pagination does not leak neighboring-topic messages
3. General-topic normalization is validated by tests and documented for live confirmation
4. Manual validation doc covers the real-forum scenarios required by the roadmap
5. `uv run pytest tests/test_tools.py -k "topic" -v` passes
</verification>

<success_criteria>
- [ ] Deleted and inaccessible topics have explicit behavior
- [ ] General-topic normalization is enforced and documented
- [ ] Topic pagination boundaries are covered by regression tests
- [ ] Live-forum validation instructions exist for 100+ topics and reverse pagination
- [ ] Phase 9 has a credible path to real-world verification beyond mocks
</success_criteria>

<output>
After completion, create `.planning/phases/09-forum-topics-support/09-03-SUMMARY.md` with:
- edge-case decisions for deleted/private/General topics
- no-leakage pagination guarantees
- manual validation checklist and any unresolved live-data assumptions
</output>
