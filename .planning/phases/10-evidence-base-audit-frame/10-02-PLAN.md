---
phase: 10-evidence-base-audit-frame
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-evidence-base-audit-frame/10-BROWNFIELD-BASELINE.md
autonomous: true
requirements:
  - EVID-01

must_haves:
  truths:
    - "The Phase 10 baseline freezes the current MCP surface from code, tests, and live reflection rather than stale notes"
    - "Brownfield constraints include read-only scope, reflection-based exposure, text-first results, workflow burden, mixed pagination, and privacy-safe telemetry"
    - "The baseline is useful to Phase 11 as an audit starting point, not just a code inventory"
  artifacts:
    - path: ".planning/phases/10-evidence-base-audit-frame/10-BROWNFIELD-BASELINE.md"
      provides: "Current-surface inventory, workflow burden notes, preserved invariants, and code anchors for the later audit"
      exports: ["Public tool inventory", "Workflow baseline", "Preserved invariants", "Code anchors"]
  key_links:
    - from: "server.py reflection path"
      to: "10-BROWNFIELD-BASELINE.md"
      via: "captures how tools are discovered and why stale notes must not define the surface"
      pattern: "inspect.getmembers|ToolArgs|list_tools"
    - from: "tools.py handler behavior"
      to: "10-BROWNFIELD-BASELINE.md"
      via: "captures pagination, recovery, and workflow choreography as part of the public contract"
      pattern: "next_cursor|next_offset|ListTopics|Retry"
    - from: "analytics.py and cache.py"
      to: "10-BROWNFIELD-BASELINE.md"
      via: "captures stateful and privacy-safe invariants that later redesigns should preserve"
      pattern: "telemetry|message content|xdg_state_home|cache"
---

<objective>
Freeze the current `mcp-telegram` MCP surface as the brownfield baseline for later audit work.

Purpose: document what the model-facing contract actually looks like today, including tool inventory,
workflow burden, result conventions, recovery paths, pagination divergence, and preserved invariants.
Output: `10-BROWNFIELD-BASELINE.md` with code/runtime anchors that Phase 11 can audit directly.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-evidence-base-audit-frame/10-CONTEXT.md
@.planning/phases/10-evidence-base-audit-frame/10-RESEARCH.md
@.planning/phases/10-evidence-base-audit-frame/10-VALIDATION.md
@src/mcp_telegram/server.py
@src/mcp_telegram/telegram.py
@src/mcp_telegram/tools.py
@src/mcp_telegram/resolver.py
@src/mcp_telegram/formatter.py
@src/mcp_telegram/cache.py
@src/mcp_telegram/analytics.py
@tests/test_formatter.py
@tests/test_resolver.py
@tests/test_analytics.py
@tests/privacy_audit.sh
@tests/test_tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the reflected public inventory and metadata path</name>
  <files>.planning/phases/10-evidence-base-audit-frame/10-BROWNFIELD-BASELINE.md</files>
  <action>
Create `10-BROWNFIELD-BASELINE.md` and open with the current public surface.

Required points:
- list all reflected public tools
- note that `ListTopics` is part of the current surface
- explain that discovery is reflection-based from `ToolArgs` subclasses in `server.py`
- explain that the tool map is snapshotted at process start rather than refreshed dynamically
- note that prompts, resources, and resource templates are currently empty
- explain that tool descriptions come from docstrings and Pydantic schema via `_sanitize_tool_schema()`
- record that uncaught handler failures are wrapped as generic `Tool <name> failed` errors at the server boundary

Anchor each point to a concrete file or runtime command.
  </action>
  <verify>
    <automated>rg -n "ListTopics|reflection-based|snapshotted|Tool <name> failed|list_prompts|list_resources|list_resource_templates|_sanitize_tool_schema" .planning/phases/10-evidence-base-audit-frame/10-BROWNFIELD-BASELINE.md</automated>
  </verify>
  <done>The baseline captures the reflected tool inventory and the metadata/discovery path that exposes it.</done>
</task>

<task type="auto">
  <name>Task 2: Document workflow burden, result conventions, and recovery style</name>
  <files>.planning/phases/10-evidence-base-audit-frame/10-BROWNFIELD-BASELINE.md</files>
  <action>
Add sections that describe the current model-facing contract beyond raw tool names.

Minimum coverage:
- result bodies are text-first and usually single `TextContent`
- recovery is action-oriented rather than opaque
- ambiguity handling relies on resolver-driven retry guidance
- discovery and forum flows can require multi-step choreography, such as `ListDialogs -> ListTopics -> ListMessages`
- `ListDialogs` defaults to mixed archived + non-archived scope and exposes `exclude_archived` plus `ignore_pinned`
- `ListTopics` exposes topic status distinctions such as `general`, `active`, and `previously_inaccessible`
- `ListMessages` supports forward-in-time pagination via `from_beginning=True`
- `ListMessages` preserves deleted-topic and inaccessible-topic recovery paths
- `SearchMessages` returns hit-centric groups with `+-3` context windows and explicit `[HIT]` marking
- the current surface mixes pagination conventions (`next_cursor` vs `next_offset`)

Use concise prose plus a compact table if helpful. The goal is to seed Phase 11's audit, not to
repeat every handler line-by-line.
  </action>
  <verify>
    <automated>rg -n "TextContent|action-oriented|ListDialogs -> ListTopics -> ListMessages|exclude_archived|ignore_pinned|previously_inaccessible|from_beginning|\\[HIT\\]|next_cursor|next_offset|ambiguity" .planning/phases/10-evidence-base-audit-frame/10-BROWNFIELD-BASELINE.md</automated>
  </verify>
  <done>The baseline explains current workflow and continuation burden in model-facing terms.</done>
</task>

<task type="auto">
  <name>Task 3: Record preserved invariants and stateful constraints</name>
  <files>.planning/phases/10-evidence-base-audit-frame/10-BROWNFIELD-BASELINE.md</files>
  <action>
Add a preserved-invariants section with the constraints later redesign options should treat as
default-preserve unless evidence justifies changing them.

Minimum invariants:
- Telegram access remains read-only
- cached client and XDG-backed cache/analytics DBs make the system stateful
- privacy-safe telemetry exists and intentionally avoids message-content logging
- recovery-critical cached metadata should not be casually removed if it materially lowers agent burden
- formatter, resolver, analytics, and privacy-audit tests are evidence for the shipped contract, not optional extras

This section should be explicit enough that Phase 12 can compare redesign options against it.
  </action>
  <verify>
    <automated>rg -n "read-only|XDG|analytics|message-content|stateful|privacy_audit|formatter|resolver|invariant" .planning/phases/10-evidence-base-audit-frame/10-BROWNFIELD-BASELINE.md</automated>
  </verify>
  <done>The baseline names the invariants and stateful constraints later phases must preserve or justify breaking.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. The baseline lists the reflected seven-tool surface
2. The discovery and metadata path is tied to `server.py` and `tools.py`
3. Workflow burden and pagination divergence are documented explicitly, including `from_beginning` and `[HIT]`
4. Archived-scope defaults, topic statuses, and topic recovery paths are frozen explicitly
5. Preserved invariants include read-only scope, cached-client statefulness, and privacy-safe telemetry
</verification>

<success_criteria>
- [ ] `10-BROWNFIELD-BASELINE.md` exists
- [ ] The current public surface is frozen from runtime/code reality
- [ ] Workflow burden and recovery semantics are documented in model-facing terms
- [ ] Search context-window, hit-marker, archived-scope, and topic-status behaviors are frozen explicitly
- [ ] Preserved invariants and stateful constraints are explicit
</success_criteria>

<output>
After completion, create `.planning/phases/10-evidence-base-audit-frame/10-02-SUMMARY.md` with:
- reflected tool inventory
- key workflow-burden notes
- preserved invariants
- any mismatches found between stale notes and runtime reality
</output>
