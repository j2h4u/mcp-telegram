---
phase: 10-evidence-base-audit-frame
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - .planning/phases/10-evidence-base-audit-frame/10-AUDIT-FRAME.md
autonomous: true
requirements:
  - EVID-01

must_haves:
  truths:
    - "The final Phase 10 artifact defines the exact rubric later phases should use rather than leaving audit criteria implicit"
    - "The rubric evaluates both individual tools and multi-step workflows"
    - "The audit frame uses the required dimensions and non-numeric judgment bands without pretending to precision"
  artifacts:
    - path: ".planning/phases/10-evidence-base-audit-frame/10-AUDIT-FRAME.md"
      provides: "Audit rubric, judgment bands, evaluation instructions, preserved invariants, and handoff rules for Phases 11-13"
      exports: ["Audit rubric", "Workflow coverage expectations", "Judgment bands", "Later-phase handoff"]
  key_links:
    - from: "10-EVIDENCE-LOG.md"
      to: "10-AUDIT-FRAME.md"
      via: "ensures every audit dimension is grounded in retained evidence rather than intuition"
      pattern: "Source|Later consumers|Primary external"
    - from: "10-BROWNFIELD-BASELINE.md"
      to: "10-AUDIT-FRAME.md"
      via: "ensures later audit work measures the actual current surface and preserved invariants"
      pattern: "Public tool inventory|Preserved invariants|next_cursor"
    - from: "Phase 10 roadmap success criteria"
      to: "10-AUDIT-FRAME.md"
      via: "ensures the final frame covers all required dimensions and brownfield constraints"
      pattern: "task-shape fit|metadata/schema clarity|ambiguity recovery"
---

<objective>
Turn the retained evidence and brownfield baseline into the reusable audit frame for later phases.

Purpose: define the rubric, judgment bands, workflow coverage expectations, and handoff guidance that
Phases 11-13 will consume directly.
Output: `10-AUDIT-FRAME.md` that is explicit enough for a future maintainer to run the audit without
re-deriving the methodology.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-evidence-base-audit-frame/10-CONTEXT.md
@.planning/phases/10-evidence-base-audit-frame/10-RESEARCH.md
@.planning/phases/10-evidence-base-audit-frame/10-VALIDATION.md
@.planning/phases/10-evidence-base-audit-frame/10-EVIDENCE-LOG.md
@.planning/phases/10-evidence-base-audit-frame/10-BROWNFIELD-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the rubric and judgment bands</name>
  <files>.planning/phases/10-evidence-base-audit-frame/10-AUDIT-FRAME.md</files>
  <action>
Create `10-AUDIT-FRAME.md` with a rubric table or equivalent structured section.

Required dimensions:
- task-shape fit
- metadata/schema clarity
- continuation burden
- ambiguity recovery
- structured-output expectations

Required judgment bands:
- `strong`
- `mixed`
- `weak`

For each dimension, include:
- what the evaluator should look for
- the kind of brownfield evidence to cite
- what a `strong`, `mixed`, and `weak` judgment means in this project

Make sure the rubric explicitly accounts for:
- discovery freshness limits caused by snapshotted tool enumeration
- generic server-boundary failure wrapping (`Tool <name> failed`) under recovery/error-flow analysis
  </action>
  <verify>
    <automated>rg -n "task-shape fit|metadata/schema clarity|continuation burden|ambiguity recovery|structured-output expectations|strong|mixed|weak|Tool <name> failed|discovery freshness" .planning/phases/10-evidence-base-audit-frame/10-AUDIT-FRAME.md</automated>
  </verify>
  <done>The audit frame contains the full rubric and judgment bands required by Phase 10.</done>
</task>

<task type="auto">
  <name>Task 2: Add tool-level and workflow-level audit instructions</name>
  <files>.planning/phases/10-evidence-base-audit-frame/10-AUDIT-FRAME.md</files>
  <action>
Add explicit instructions for Phase 11 so the audit covers both:

- each current public tool
- the main user workflows for discovery, reading, search, topic handling, and recovery/error flows

The instructions should tell the executor to pair named evidence with concrete `mcp-telegram`
behaviors and to avoid findings that cite only generic best-practice prose.
  </action>
  <verify>
    <automated>rg -n "each current public tool|discovery|reading|search|topic handling|recovery/error flows|named evidence" .planning/phases/10-evidence-base-audit-frame/10-AUDIT-FRAME.md</automated>
  </verify>
  <done>The audit frame explicitly instructs later work to audit tools and workflows, not only handlers.</done>
</task>

<task type="auto">
  <name>Task 3: Write the handoff rules for Phases 11-13</name>
  <files>.planning/phases/10-evidence-base-audit-frame/10-AUDIT-FRAME.md</files>
  <action>
Finish the artifact with a short handoff section that tells later phases how to use Phase 10 outputs.

Minimum guidance:
- Phase 11 must cite the evidence log and brownfield baseline in every major finding
- Phase 12 must preserve or explicitly challenge the baseline invariants when comparing redesign options
- Phase 13 must treat the evidence log and audit frame as inputs to the decision memo instead of redoing the audit methodology

Keep this section practical and short.
  </action>
  <verify>
    <automated>rg -n "Phase 11|Phase 12|Phase 13|evidence log|brownfield baseline|decision memo" .planning/phases/10-evidence-base-audit-frame/10-AUDIT-FRAME.md</automated>
  </verify>
  <done>The audit frame ends with clear handoff rules for the remaining v1.2 phases.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. The audit frame uses all five required dimensions
2. The rubric is non-numeric and uses `strong/mixed/weak`
3. The instructions explicitly require tool-level and workflow-level audit coverage
4. The handoff section names Phases 11, 12, and 13 explicitly
</verification>

<success_criteria>
- [ ] `10-AUDIT-FRAME.md` exists
- [ ] The rubric covers all required dimensions and judgment bands
- [ ] Later phases receive explicit instructions about evidence use and coverage scope
- [ ] The artifact is reusable without re-deriving Phase 10 methodology
</success_criteria>

<output>
After completion, create `.planning/phases/10-evidence-base-audit-frame/10-03-SUMMARY.md` with:
- final rubric dimensions
- judgment-band definitions
- workflow coverage expectations
- phase handoff rules
</output>
