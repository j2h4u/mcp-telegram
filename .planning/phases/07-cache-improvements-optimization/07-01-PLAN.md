---
phase: 07-cache-improvements-optimization
plan: 01
type: auto
wave: 1
depends_on: []
files_modified: [src/mcp_telegram/cache.py, tests/test_cache.py]
autonomous: true
requirements: [CACHE-01]
must_haves:
  truths:
    - "SQLite indexes exist on entity_cache.db: idx_entities_type_updated(type, updated_at) and idx_entities_username(username)"
    - "TTL queries use the index (EXPLAIN QUERY PLAN shows SEARCH, not SCAN)"
    - "Username lookups use the index (EXPLAIN QUERY PLAN shows SEARCH, not SCAN)"
  artifacts:
    - path: src/mcp_telegram/cache.py
      provides: Index creation in EntityCache.__init__(), CREATE INDEX IF NOT EXISTS statements
      min_lines: 50
    - path: tests/test_cache.py
      provides: "Test functions: test_indexes_created, test_ttl_query_uses_index, test_username_index_used"
      min_lines: 30
  key_links:
    - from: src/mcp_telegram/cache.py
      to: SQLite CREATE INDEX statements
      via: EntityCache.__init__() after table creation
      pattern: "CREATE INDEX IF NOT EXISTS idx_entities"
    - from: tests/test_cache.py
      to: EXPLAIN QUERY PLAN verification
      via: cursor.execute() on test queries
      pattern: "EXPLAIN QUERY PLAN"
---

<objective>
Add SQLite indexes to entity_cache.db to optimize TTL filtering and username lookups from O(N) to O(log N).

Purpose: entity_cache.db queries (all_names_with_ttl, get_by_username) currently scan all rows; indexes enable SQLite query planner to seek directly to relevant rows.
Output: Index creation code in cache.py, verification tests in test_cache.py demonstrating index usage via EXPLAIN QUERY PLAN.
</objective>

<execution_context>
@/home/j2h4u/.claude/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-cache-improvements-optimization/07-RESEARCH.md
@src/mcp_telegram/cache.py
@tests/test_cache.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQLite indexes in EntityCache.__init__()</name>
  <files>src/mcp_telegram/cache.py</files>
  <action>
    In EntityCache.__init__(), after executing _DDL (table creation) and before commit, add two CREATE INDEX IF NOT EXISTS statements:
    1. `CREATE INDEX IF NOT EXISTS idx_entities_type_updated ON entities(type, updated_at)` — for TTL filtering in all_names_with_ttl()
    2. `CREATE INDEX IF NOT EXISTS idx_entities_username ON entities(username)` — for username lookups in get_by_username()

    Call PRAGMA optimize after index creation to rebuild statistics so query planner uses the indexes. Use the pattern from research document (Section "Code Examples: Index Creation at Startup").

    **Why:** Indexes must exist at database creation time (or early connection). CREATE INDEX IF NOT EXISTS is idempotent, safe on subsequent runs.
  </action>
  <verify>
    <automated>pytest tests/test_cache.py::test_indexes_created -xvs</automated>
  </verify>
  <done>Index creation code added to cache.py EntityCache.__init__(); both indexes created after DDL, PRAGMA optimize called; indexes visible in SQLite schema.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests verifying index usage via EXPLAIN QUERY PLAN</name>
  <files>tests/test_cache.py</files>
  <action>
    Add three test functions to test_cache.py to verify indexes are actually used by SQLite:

    1. `test_indexes_created(tmp_db_path)` — Open database, query sqlite_master table, verify both idx_entities_type_updated and idx_entities_username exist (names and on_table = 'entities').

    2. `test_ttl_query_uses_index(tmp_db_path)` — Run EXPLAIN QUERY PLAN on the query from all_names_with_ttl():
       `SELECT id, name FROM entities WHERE (type = 'user' AND updated_at >= ?) OR (type != 'user' AND updated_at >= ?)`
       Parse the output and verify it contains "SEARCH entities USING INDEX idx_entities_type_updated" (not "SCAN TABLE entities").

    3. `test_username_index_used(tmp_db_path)` — Run EXPLAIN QUERY PLAN on the query from get_by_username():
       `SELECT id, name FROM entities WHERE username = ?`
       Verify output contains "SEARCH entities USING INDEX idx_entities_username".

    Use the verification pattern from research (Section "Code Examples: TTL Index Query Verification"). Insert test data before running EXPLAIN queries (at least one entity of each type for comprehensive plan).

    **Why:** EXPLAIN QUERY PLAN proves SQLite uses the index. If plan shows SCAN, the index is ignored and needs investigation (column order, stats).
  </action>
  <verify>
    <automated>pytest tests/test_cache.py::test_ttl_query_uses_index tests/test_cache.py::test_username_index_used -xvs</automated>
  </verify>
  <done>Three test functions added and passing; EXPLAIN QUERY PLAN output shows index usage (SEARCH with correct index name); no SCAN TABLE in plan output.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. Index creation code in cache.py compiles and creates indexes on first connection
2. All three new tests pass: indexes exist, TTL query uses idx_entities_type_updated, username query uses idx_entities_username
3. Existing cache tests (test_persistence, test_ttl_expiry, etc.) remain green
4. Database file can be opened with sqlite3 CLI and `SELECT * FROM sqlite_master WHERE type='index'` shows two new indexes
</verification>

<success_criteria>
- [ ] Both indexes (idx_entities_type_updated, idx_entities_username) created in entity_cache.db
- [ ] EXPLAIN QUERY PLAN confirms SEARCH with correct index name for all_names_with_ttl() and get_by_username()
- [ ] All 3 new tests pass + all existing cache tests pass
- [ ] No regression in other test suites (test_resolver.py, test_tools.py still green)
- [ ] Code follows existing cache.py patterns: minimal, clear, well-commented
</success_criteria>

<output>
After completion, create `.planning/phases/07-cache-improvements-optimization/07-01-SUMMARY.md` with:
- Indexes created: names, columns, purpose
- Test results: 3 new tests + 5 existing tests passing
- EXPLAIN QUERY PLAN sample output (one from each index)
- Performance: note that index creation adds <1ms to startup (one-time); query speedup verified in Phase 7 load test (Wave 2)
</output>
