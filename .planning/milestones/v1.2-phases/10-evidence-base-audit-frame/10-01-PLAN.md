---
phase: 10-evidence-base-audit-frame
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-evidence-base-audit-frame/10-EVIDENCE-LOG.md
autonomous: true
requirements:
  - EVID-01

must_haves:
  truths:
    - "Phase 10 retains a narrow evidence set rather than a generic MCP literature review"
    - "The evidence log explicitly separates primary external guidance from brownfield authority and weaker contextual sources"
    - "Each retained source says why it applies to `mcp-telegram` and which later phases should rely on it"
  artifacts:
    - path: ".planning/phases/10-evidence-base-audit-frame/10-EVIDENCE-LOG.md"
      provides: "Decision-oriented evidence matrix with source tiers, applicability notes, and later-phase reuse guidance"
      exports: ["Primary external sources", "Brownfield authority sources", "Evidence matrix"]
  key_links:
    - from: "MCP Tools specification"
      to: "10-EVIDENCE-LOG.md"
      via: "documents why model-controlled tool discovery and invocation rules apply to the current server surface"
      pattern: "MCP Tools spec|tools/list|tools/call"
    - from: "Anthropic tool-use docs"
      to: "10-EVIDENCE-LOG.md"
      via: "documents why tool descriptions, schema clarity, and structured-output expectations belong in the audit frame"
      pattern: "Anthropic|description|schema|structured"
    - from: "live reflected tool list"
      to: "10-EVIDENCE-LOG.md"
      via: "freezes the current public surface from runtime reality instead of stale notes"
      pattern: "ListTopics|GetMyAccount|GetUsageStats"
---

<objective>
Create the retained evidence base for Phase 10 in one compact artifact.

Purpose: give later phases a source-ranked, applicability-aware evidence log that distinguishes
normative MCP/Anthropic guidance from local code/runtime authority and avoids drifting into a vague
survey.
Output: `10-EVIDENCE-LOG.md` with source tiers, applicability notes, and explicit reuse guidance for
Phases 11-13.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-evidence-base-audit-frame/10-CONTEXT.md
@.planning/phases/10-evidence-base-audit-frame/10-RESEARCH.md
@.planning/phases/10-evidence-base-audit-frame/10-VALIDATION.md
@src/mcp_telegram/server.py
@src/mcp_telegram/telegram.py
@src/mcp_telegram/tools.py
@tests/test_formatter.py
@tests/test_resolver.py
@tests/test_analytics.py
@tests/privacy_audit.sh
@tests/test_tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the source hierarchy and retained-source rule</name>
  <files>.planning/phases/10-evidence-base-audit-frame/10-EVIDENCE-LOG.md</files>
  <action>
Create `10-EVIDENCE-LOG.md` with a short methodology section that:

- defines source tiers as `Primary external`, `Brownfield authority`, `Supporting official`, and `Context only`
- states that official MCP and Anthropic docs are normative for external guidance
- states that live reflection, source code, and tests are authoritative for current `mcp-telegram` behavior
- states the retention rule: only keep sources that materially shape later audit or redesign conclusions
- records what to do when no source from a weaker tier is retained: say so explicitly rather than
  silently collapsing the tier

Keep this section short. The goal is an audit input, not a survey paper.
  </action>
  <verify>
    <automated>rg -n "Primary external|Brownfield authority|Supporting official|Context only|retention rule" .planning/phases/10-evidence-base-audit-frame/10-EVIDENCE-LOG.md</automated>
  </verify>
  <done>`10-EVIDENCE-LOG.md` opens with a clear source hierarchy and retention rule.</done>
</task>

<task type="auto">
  <name>Task 2: Build the evidence matrix with applicability and reuse notes</name>
  <files>.planning/phases/10-evidence-base-audit-frame/10-EVIDENCE-LOG.md</files>
  <action>
Add a compact table that retains only the minimum source set Phase 10 needs.

Minimum rows to include:
- MCP Tools specification
- Anthropic implement-tool-use doc
- Anthropic tool-use overview
- live reflected tool list from `uv run cli.py list-tools`
- `src/mcp_telegram/server.py`
- `src/mcp_telegram/telegram.py`
- `src/mcp_telegram/tools.py`
- `src/mcp_telegram/resolver.py`
- `src/mcp_telegram/formatter.py`
- `src/mcp_telegram/cache.py`
- `src/mcp_telegram/analytics.py`
- `tests/test_formatter.py`
- `tests/test_resolver.py`
- `tests/test_analytics.py`
- `tests/privacy_audit.sh`
- `tests/test_tools.py`

Required columns:
- Source
- Tier
- Area informed
- Why it applies to `mcp-telegram`
- Later consumers

The `Why it applies` cell must mention a concrete project concern, such as reflection-based discovery,
cached-client statefulness, text-first results, recovery burden, pagination divergence, or privacy-safe telemetry.
  </action>
  <verify>
    <automated>rg -n "Why it applies|Later consumers|MCP Tools specification|Anthropic implement-tool-use|src/mcp_telegram/tools.py" .planning/phases/10-evidence-base-audit-frame/10-EVIDENCE-LOG.md</automated>
  </verify>
  <done>The evidence matrix records each retained source, why it matters here, and which later phases should reuse it.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm runtime reality is represented explicitly</name>
  <files>.planning/phases/10-evidence-base-audit-frame/10-EVIDENCE-LOG.md</files>
  <action>
Add a short brownfield-authority note or subsection that records the reflected current surface from
`uv run cli.py list-tools`, including the fact that `ListTopics` is exposed today.

Do not turn this into the full brownfield baseline; that belongs to Plan 02. This plan only needs
enough runtime context to prevent stale-tool-list drift in later phases.
  </action>
  <verify>
    <automated>rg -n "ListTopics|GetMyAccount|GetUsageStats|live reflected tool list" .planning/phases/10-evidence-base-audit-frame/10-EVIDENCE-LOG.md</automated>
  </verify>
  <done>The evidence log includes the reflected runtime inventory as brownfield authority.</done>
</task>

<task type="auto">
  <name>Task 4: Preserve explicit weaker-tier separation even when sparse</name>
  <files>.planning/phases/10-evidence-base-audit-frame/10-EVIDENCE-LOG.md</files>
  <action>
Add a brief note or subsection for `Supporting official` and `Context only` evidence.

If no source from one of those tiers is retained, say that explicitly. Do not silently omit the tier,
because Phase 10 is responsible for distinguishing authoritative guidance from weaker sources even when
the retained set is intentionally narrow.
  </action>
  <verify>
    <automated>rg -n "Supporting official|Context only|none retained|secondary|community" .planning/phases/10-evidence-base-audit-frame/10-EVIDENCE-LOG.md</automated>
  </verify>
  <done>The evidence log preserves explicit weaker-tier separation instead of implying it away.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. The evidence log distinguishes source tiers explicitly
2. Each retained source includes a project-specific applicability note
3. The reflected runtime tool list appears in the evidence base
4. The artifact names later consumers for Phases 11-13
5. Supporting-official and context/community tiers are either populated or explicitly marked unneeded
</verification>

<success_criteria>
- [ ] `10-EVIDENCE-LOG.md` exists and is compact rather than survey-like
- [ ] The source hierarchy distinguishes official external guidance from local authority
- [ ] Every retained source records why it applies to `mcp-telegram`
- [ ] The live reflected tool list is captured explicitly
- [ ] Supporting-official and weaker contextual tiers are explicitly represented, even if empty
</success_criteria>

<output>
After completion, create `.planning/phases/10-evidence-base-audit-frame/10-01-SUMMARY.md` with:
- retained source list
- source-tier definitions
- runtime inventory note
- any sources intentionally excluded as non-material
</output>
