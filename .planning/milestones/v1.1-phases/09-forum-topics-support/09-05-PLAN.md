---
phase: 09-forum-topics-support
plan: 05
type: execute
mode: gap_closure
wave: 5
depends_on: [09-04]
files_modified:
  - src/mcp_telegram/tools.py
  - tests/conftest.py
  - tests/test_tools.py
autonomous: true
requirements:
  - TOPIC-01
  - TOPIC-02

must_haves:
  truths:
    - "`topic + unread` remains topic-scoped for both General and non-General topics"
    - "General-topic unread behavior is no longer a dialog-wide unread fetch disguised as a topic filter"
    - "Unread cursors are derived from emitted topic-scoped messages, not from unrelated dialog messages"
  artifacts:
    - path: "src/mcp_telegram/tools.py"
      provides: "Topic-aware unread fetch logic and cursor handling for General and non-General topics"
      exports: ["_message_matches_topic", "_fetch_topic_messages", "list_messages"]
    - path: "tests/conftest.py"
      provides: "Fixtures for General-topic unread pages and adjacent-topic leak scenarios"
      exports:
        - "make_general_topic_message"
        - "make_mock_forum_reply"
    - path: "tests/test_tools.py"
      provides: "Live-representative regressions for topic+unread scoping, General filtering, and cursor stability"
      exports:
        - "test_list_messages_general_topic_unread_is_scoped"
        - "test_list_messages_topic_unread_cursor_is_topic_scoped"
        - "test_list_messages_topic_unread_filters_dialog_leaks"
  key_links:
    - from: "GetPeerDialogsRequest unread lower bound"
      to: "topic-aware page fetch"
      via: "read_inbox_max_id stays the lower boundary, but a topic predicate still decides which messages count toward the page"
      pattern: "read_inbox_max_id"
    - from: "General topic metadata"
      to: "General unread filtering"
      via: "General uses explicit filtering rules rather than an implicit unfiltered iter_messages call"
      pattern: "is_general"
    - from: "topic-scoped emitted messages"
      to: "next_cursor"
      via: "cursor state is derived from the last message actually returned to the caller"
      pattern: "next_cursor"
---

<objective>
Fix the live behavioral gap where `topic + unread` may ignore topic scoping, especially for the General topic.

Purpose: ensure unread mode preserves the requested topic boundary instead of reusing dialog-wide unread semantics.
Output: topic-aware unread fetching and cursor generation with regression coverage based on the live symptom from 09-VERIFICATION.

Because this plan changes runtime behavior in `src/mcp_telegram/tools.py`, it is not complete on local pytest alone. Close the plan only after the `mcp-telegram` container is rebuilt, restarted, and checked from inside the running container.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-forum-topics-support/09-RESEARCH.md
@.planning/phases/09-forum-topics-support/09-VERIFICATION.md
@.planning/phases/09-forum-topics-support/09-04-PLAN.md
@src/mcp_telegram/tools.py
@tests/conftest.py
@tests/test_tools.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add live-representative regressions for topic-scoped unread behavior</name>
  <files>tests/conftest.py, tests/test_tools.py, src/mcp_telegram/tools.py</files>
  <action>
Add the tests that would have caught the live issue before Phase 9 was marked complete.

Required regressions:
- `test_list_messages_general_topic_unread_is_scoped` — `ListMessages(topic="General", unread=True)` returns only General-topic messages and does not mirror the dialog-wide unread result.
- `test_list_messages_topic_unread_cursor_is_topic_scoped` — the unread cursor comes from the last emitted topic message, not from an unrelated dialog message that happened to be in the raw fetch.
- `test_list_messages_topic_unread_filters_dialog_leaks` — when the unread fetch returns mixed raw messages, adjacent-topic messages are stripped before formatting and before cursor generation.

Model the failing case from 09-VERIFICATION directly: the dialog-wide unread cursor and the topic-scoped unread cursor must diverge when the raw unread slice contains messages from multiple topics.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "general_topic_unread_is_scoped or topic_unread_cursor_is_topic_scoped or topic_unread_filters_dialog_leaks" -v</automated>
  </verify>
  <done>The suite reproduces the unread scoping bug and fails until topic-aware unread filtering is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Implement topic-aware unread fetch rules for General and non-General topics</name>
  <files>src/mcp_telegram/tools.py</files>
  <action>
Refactor the unread path in `list_messages` so topic filtering still owns page composition after `read_inbox_max_id` is applied.

Implementation targets:
- Keep `GetPeerDialogsRequest(...).dialogs[0].read_inbox_max_id` as the lower bound for unread mode.
- For non-General topics, continue to prefer `reply_to=top_message_id` when it works, but treat unread mode as potentially mixed input and filter emitted messages with the topic predicate before counting the page.
- For the General topic, add explicit filtering rules so only General-topic messages survive unread mode. Do not rely on the absence of `reply_to` to imply correctness.
- Continue paging until either the requested limit of topic-scoped unread messages is emitted or the unread slice is exhausted.

This is a scoping fix, not a feature expansion. Preserve the current `unread=True` contract; only remove the silent topic bypass.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "topic_unread or general_topic_unread" -v</automated>
  </verify>
  <done>Unread mode uses topic-aware filtering for both General and non-General topic requests.</done>
</task>

<task type="auto">
  <name>Task 3: Stabilize unread cursors and empty-page behavior under topic filtering</name>
  <files>src/mcp_telegram/tools.py, tests/test_tools.py</files>
  <action>
Make the unread pagination contract explicit once topic filtering is applied.

Required outcomes:
- `next_cursor` is generated from the last emitted topic-scoped message only.
- Empty topic-scoped unread results do not carry a cursor copied from the raw dialog fetch.
- Existing non-topic unread behavior remains intact.
- Existing topic pagination behavior remains intact outside unread mode.

Use the tests to pin the exact cursor behavior so future refactors cannot reintroduce dialog-wide cursor leakage.

Before marking the plan complete, rebuild and restart the long-lived runtime, then verify from inside the container that `topic + unread` stays topic-scoped in the deployed server rather than only in the local checkout.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "topic_unread_cursor or list_messages_unread_filter" -v</automated>
  </verify>
  <done>Unread cursors are stable, topic-scoped, and regression-tested.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. `topic + unread` returns only messages that belong to the requested topic
2. General-topic unread mode does not reuse the dialog-wide unread cursor or content
3. Mixed unread batches do not leak adjacent-topic messages into formatted output
4. Topic-scoped unread pages emit topic-scoped cursors only
5. `uv run pytest tests/test_tools.py -k "topic_unread or unread_filter" -v` passes
6. `docker compose -f /opt/docker/mcp-telegram/docker-compose.yml up -d --build mcp-telegram` completes and the rebuilt runtime is verified inside the `mcp-telegram` container before the plan is closed
</verification>

<success_criteria>
- [ ] The live `topic + unread` scoping gap is covered by regression tests
- [ ] General-topic unread behavior is explicitly filtered instead of implicitly unfiltered
- [ ] Adjacent-topic unread leaks are stripped before formatting and cursor generation
- [ ] Topic unread cursors are derived from emitted topic messages
- [ ] Existing unread behavior outside topic filtering remains green
- [ ] The rebuilt `mcp-telegram` runtime is restarted and verified before closure
</success_criteria>

<output>
After completion, create `.planning/phases/09-forum-topics-support/09-05-SUMMARY.md` with:
- final topic-aware unread rules
- General-topic unread handling
- cursor-generation rules for topic-scoped unread pages
</output>
