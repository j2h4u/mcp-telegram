---
phase: 17-direct-read-search-workflows
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/mcp_telegram/tools.py
  - src/mcp_telegram/capabilities.py
  - tests/test_tools.py
  - tests/test_server.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02

must_haves:
  truths:
    - "`ListMessages` can perform common direct reads without defaulting to `ListDialogs -> ListTopics -> ListMessages` when the target is already known"
    - "Forum-topic fidelity survives the direct-read surface change"
    - "Local reflection shows the intended direct-read contract clearly"
  artifacts:
    - path: "src/mcp_telegram/tools.py"
      provides: "Public `ListMessages` direct-read contract for known targets"
      exports: ["Exact selector fields or equivalent direct-read surface", "Mutual-exclusion validation", "Readable direct-read guidance"]
    - path: "src/mcp_telegram/capabilities.py"
      provides: "Adapter routing from public `ListMessages` inputs into the exact-target capability seam"
      exports: ["Exact dialog read path", "Exact topic read path", "Preserved fallback/recovery behavior"]
    - path: "tests/test_tools.py"
      provides: "Brownfield regressions for direct reads, topic fidelity, and ambiguity"
      exports: ["Known-target forum read tests", "Deleted/inaccessible topic tests", "Unread/navigation tests"]
    - path: "tests/test_server.py"
      provides: "Reflection proof for the Phase 17 `ListMessages` contract"
      exports: ["Schema assertions", "ToolArgs validation coverage"]
  key_links:
    - from: "17-RESEARCH.md"
      to: "src/mcp_telegram/tools.py"
      via: "turns the recommended direct-read contract into the main public read surface"
      pattern: "ListMessages|exact-target|known dialog|known topic"
    - from: "src/mcp_telegram/capabilities.py"
      to: "tests/test_tools.py"
      via: "proves direct forum reads still preserve tombstones, inaccessible-topic recovery, and topic-scoped navigation"
      pattern: "topic|deleted|inaccessible|General|unread|navigation"
    - from: "src/mcp_telegram/tools.py"
      to: "tests/test_server.py"
      via: "keeps the direct-read schema reflected and inspectable locally"
      pattern: "ListMessages|dialog|topic|schema"
---

<objective>
Move `ListMessages` onto the direct-read workflow shape for Phase 17.

Purpose: expose the exact-target fast path at the main read surface while preserving the forum and
ambiguity guarantees that make the current brownfield read path safe.
Output: updated `ListMessages` schema/adapter behavior, regression coverage, and local reflection
proof for the direct-read contract.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-direct-read-search-workflows/17-RESEARCH.md
@.planning/phases/17-direct-read-search-workflows/17-VALIDATION.md
@.planning/phases/17-direct-read-search-workflows/17-01-PLAN.md
@src/mcp_telegram/tools.py
@src/mcp_telegram/capabilities.py
@src/mcp_telegram/cache.py
@src/mcp_telegram/server.py
@tests/test_tools.py
@tests/test_server.py
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose exact-target direct-read entry on `ListMessages`</name>
  <files>src/mcp_telegram/tools.py, src/mcp_telegram/capabilities.py</files>
  <action>
Update `ListMessages` so callers with a known target can read directly without defaulting to
helper-first setup.

Required scope:
- add the bounded public inputs needed for known-target direct reads
- make conflicting fuzzy and exact selectors fail explicitly rather than silently choosing one
- route exact-target calls through the Phase 17 capability seam from Plan 01
- keep existing natural-name `dialog` and `topic` behavior available for exploratory and ambiguous
  cases

Guardrails:
- do not remove `ListTopics` or `ListDialogs`
- do not weaken fuzzy ambiguity handling
- do not change the shared navigation contract again
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "list_messages and (direct or topic or ambiguity or navigation)" -q && uv run pytest tests/test_capabilities.py -k "history or direct or topic" -q</automated>
  </verify>
  <done>`ListMessages` exposes a direct known-target path without losing the existing safe exploratory path.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve forum-read fidelity and prove the reflected `ListMessages` schema</name>
  <files>tests/test_tools.py, tests/test_server.py</files>
  <action>
Extend read-side regression coverage so the direct path cannot regress forum behavior.

Required coverage:
- known-target topic reads preserve deleted-topic tombstones and inaccessible-topic recovery
- `General` still avoids thread fetches and cross-topic reads still render readable inline labels
- unread, sender, and continuation behavior remain aligned with the current topic-scoped guarantees
- reflected `ListMessages` schema teaches the new direct-read workflow clearly in local tooling

Use `tests/test_server.py` and local reflection checks rather than deferring schema visibility to a
later phase.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "list_messages or topic or unread or ambiguity or direct" -q && uv run pytest tests/test_server.py -q && uv run cli.py list-tools</automated>
  </verify>
  <done>The direct-read `ListMessages` contract is preserved in behavior tests and visible in local reflection.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. `ListMessages` has a direct known-target path that no longer assumes helper-first setup
2. topic ambiguity, tombstones, inaccessible-topic recovery, and `General` semantics remain intact
3. unread and continuation behavior still respect topic-scoped invariants
4. local reflection exposes the intended direct-read contract clearly
</verification>

<success_criteria>
- [ ] `FLOW-01` is satisfied on the read side for known targets
- [ ] Direct forum reads no longer require `ListTopics` as the default path when the topic is already known
- [ ] Brownfield topic-fidelity tests still pass under the new contract
- [ ] Local reflection matches the intended `ListMessages` surface
- [ ] No helper-posture or rollout-proof scope is used as a substitute for the workflow change
</success_criteria>

<output>
After completion, create `.planning/phases/17-direct-read-search-workflows/17-02-SUMMARY.md` with:
- direct `ListMessages` workflow landed
- preserved forum/topic guarantees
- reflection and regression checks performed
- quick-test command used
</output>
