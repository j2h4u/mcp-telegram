---
phase: 17-direct-read-search-workflows
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/mcp_telegram/tools.py
  - src/mcp_telegram/capabilities.py
  - src/mcp_telegram/formatter.py
  - tests/test_tools.py
  - tests/test_capabilities.py
  - tests/test_server.py
  - tests/test_analytics.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02

must_haves:
  truths:
    - "`SearchMessages` can run common direct searches with lower orchestration burden while staying scoped to one dialog"
    - "Search hit-local context remains intact after workflow assembly moves deeper into the shared seams"
    - "The final Phase 17 reflected schemas and restarted runtime agree on the changed primary-tool contract"
  artifacts:
    - path: "src/mcp_telegram/tools.py"
      provides: "Final direct-search public contract and thin adapter behavior"
      exports: ["Existing dialog/query contract with exact-id fast path support", "Direct search guidance", "Shared footer behavior"]
    - path: "src/mcp_telegram/capabilities.py"
      provides: "Search workflow assembly below the adapter"
      exports: ["Hit-window grouping", "Preserved query/dialog-scoped navigation", "Bounded context model"]
    - path: "src/mcp_telegram/formatter.py"
      provides: "Readable search hit-window formatting support"
      exports: ["Hit marker support", "Grouped hit rendering", "Adapter-thinning helper"]
    - path: "tests/test_server.py"
      provides: "Reflection and MCP-boundary proof for the final Phase 17 search/read contract"
      exports: ["Final schema visibility", "ToolArgs validation coverage"]
    - path: "tests/test_analytics.py"
      provides: "Bounded telemetry proof after the direct-workflow migration"
      exports: ["Privacy-safe telemetry assertions", "Direct-workflow event semantics"]
  key_links:
    - from: "17-RESEARCH.md"
      to: "src/mcp_telegram/capabilities.py"
      via: "moves search workflow assembly into the capability/formatter seam identified in research"
      pattern: "SearchMessages|hit-local context|workflow assembly|direct"
    - from: "src/mcp_telegram/formatter.py"
      to: "tests/test_tools.py"
      via: "proves grouped hit output stays readable after the adapter stops hand-building it"
      pattern: "HIT|context|hit|format"
    - from: "src/mcp_telegram/tools.py"
      to: "tests/test_analytics.py"
      via: "keeps telemetry bounded and privacy-safe after the direct-search contract lands"
      pattern: "SearchMessages|telemetry|has_cursor|privacy"
---

<objective>
Finish Phase 17 by moving `SearchMessages` onto the direct-workflow shape and proving the changed
primary-tool contract locally and in the restarted runtime.

Purpose: reduce search orchestration burden, keep hit-local fidelity intact, and complete the
runtime-sensitive contract update required by the repo's execution discipline.
Output: final direct-search surface, capability/formatter workflow shaping, regression coverage,
telemetry proof, and restarted-runtime verification.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-direct-read-search-workflows/17-RESEARCH.md
@.planning/phases/17-direct-read-search-workflows/17-VALIDATION.md
@.planning/phases/17-direct-read-search-workflows/17-02-PLAN.md
@src/mcp_telegram/tools.py
@src/mcp_telegram/capabilities.py
@src/mcp_telegram/formatter.py
@src/mcp_telegram/analytics.py
@src/mcp_telegram/server.py
@cli.py
@tests/test_tools.py
@tests/test_capabilities.py
@tests/test_server.py
@tests/test_analytics.py
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose direct search entry and move hit-window assembly below the adapter</name>
  <files>src/mcp_telegram/tools.py, src/mcp_telegram/capabilities.py, src/mcp_telegram/formatter.py</files>
  <action>
Update `SearchMessages` so common known-target searches are more direct and the adapter stops
hand-building search workflow output.

Required scope:
- keep the current `dialog` + `query` public contract unless implementation proves it cannot carry
  the direct path cleanly
- use exact numeric dialog ids or equivalent exact-target signals behind the existing contract to
  route known targets through the Phase 17 capability seam from Plan 01
- move grouped hit-window assembly, hit-marker support, and other search-specific rendering support
  into capability/formatter helpers instead of rebuilding them inline in `tools.py`
- preserve current dialog scoping, query-scoped navigation validation, hit-local context radius,
  and readable grouped output

Guardrails:
- do not broaden search beyond one dialog
- do not change the shared navigation vocabulary again
- do not add speculative new public fields if the existing contract can express the direct path
- do not widen telemetry payloads
  </action>
  <verify>
    <automated>uv run pytest tests/test_capabilities.py -k "search or direct or navigation" -q && uv run pytest tests/test_tools.py -k "search_messages or hit or context or navigation or direct" -q</automated>
  </verify>
  <done>`SearchMessages` now supports direct known-dialog searches and keeps search workflow assembly below the adapter.</done>
</task>

<task type="auto">
  <name>Task 2: Update schema and telemetry coverage for the final Phase 17 primary-tool contract</name>
  <files>tests/test_tools.py, tests/test_server.py, tests/test_analytics.py</files>
  <action>
Add the final regression, reflection, and bounded-telemetry proof for Phase 17.

Required coverage:
- reflected schemas for `ListMessages` and `SearchMessages` expose the intended direct-workflow
  entry points clearly
- search output still marks hits distinctly and keeps bounded local context
- any telemetry semantics touched by the direct-workflow migration remain privacy-safe and bounded
- the final contract still does not rely on helper-surface posture decisions to feel coherent

Prefer targeted telemetry checks over broader analytics redesign.
  </action>
  <verify>
    <automated>uv run pytest tests/test_server.py tests/test_analytics.py -q && uv run pytest tests/test_tools.py -k "search_messages or list_messages or telemetry or schema or direct" -q && uv run cli.py list-tools</automated>
  </verify>
  <done>The final Phase 17 public contract is proven locally and telemetry remains privacy-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild and restart the runtime to prove the direct-workflow contract is live</name>
  <files>src/mcp_telegram/tools.py, src/mcp_telegram/capabilities.py, src/mcp_telegram/formatter.py</files>
  <action>
Because this phase changes reflected primary-tool schemas, update the long-lived runtime on this
machine instead of relying on repo-local checks alone.

Required proof:
- capture local reflected tool schemas with `uv run cli.py list-tools`
- rebuild and restart `mcp-telegram` using the documented compose file
- verify inside the container that the updated code imports successfully
- confirm the restarted runtime exposes the intended final schemas for `ListMessages` and
  `SearchMessages`
  </action>
  <verify>
    <automated>uv run cli.py list-tools && docker compose -f /opt/docker/mcp-telegram/docker-compose.yml up -d --build mcp-telegram && docker exec mcp-telegram /opt/venv/bin/python -c "import json; from mcp_telegram.tools import ListMessages, SearchMessages, tool_description; print(json.dumps({'ListMessages': tool_description(ListMessages).inputSchema, 'SearchMessages': tool_description(SearchMessages).inputSchema}, ensure_ascii=True, sort_keys=True))"</automated>
  </verify>
  <done>The restarted runtime is confirmed to expose the final Phase 17 direct-workflow contract.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. `SearchMessages` supports a direct known-target entry without helper-first setup
2. hit-local context, hit markers, and query/dialog-scoped navigation remain intact
3. the public adapters stay thin relative to the capability/formatter seam
4. telemetry remains privacy-safe after the direct-workflow migration
5. local reflection and restarted runtime agree on the final Phase 17 contract
</verification>

<success_criteria>
- [ ] `FLOW-02` is satisfied on the search side
- [ ] Search output remains hit-local and readable after the workflow reshaping
- [ ] Local reflection and restarted runtime expose the intended final Phase 17 schemas
- [ ] Telemetry remains bounded and privacy-safe
- [ ] The repo is ready for Phase 18 helper-posture and rollout-proof work on top of the new primary workflows
</success_criteria>

<output>
After completion, create `.planning/phases/17-direct-read-search-workflows/17-03-SUMMARY.md` with:
- direct `SearchMessages` workflow landed
- hit-window assembly moved below the adapter
- telemetry/reflection checks performed
- runtime rebuild/restart verification used
</output>
