---
phase: 17-direct-read-search-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_telegram/capabilities.py
  - src/mcp_telegram/cache.py
  - tests/test_capabilities.py
  - tests/test_tools.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02

must_haves:
  truths:
    - "Phase 17 introduces exact-target capability lanes before the public tool adapters change"
    - "Known dialog/topic identifiers can enter the existing read/search capability flow without weakening name-based ambiguity handling"
    - "Topic tombstones, inaccessible-topic recovery, and scoped navigation remain intact under the fast-path foundation"
  artifacts:
    - path: "src/mcp_telegram/capabilities.py"
      provides: "Shared exact-target capability entrypoints for direct read/search workflows"
      exports: ["Exact dialog targeting", "Exact topic targeting", "Preserved name-based fallback paths"]
    - path: "src/mcp_telegram/cache.py"
      provides: "Cache-backed topic metadata access needed for direct forum reads"
      exports: ["Exact topic lookup", "Tombstone reuse", "Bounded metadata refresh support"]
    - path: "tests/test_capabilities.py"
      provides: "Capability-level anchors for exact-target fast paths"
      exports: ["Direct read tests", "Direct search tests", "Ambiguity-preservation tests"]
    - path: "tests/test_tools.py"
      provides: "Brownfield contract anchors proving helper flows are reduced only for exact known targets"
      exports: ["No silent auto-resolution", "Topic-fidelity regression checks", "Known-target workflow checks"]
  key_links:
    - from: "17-RESEARCH.md"
      to: "src/mcp_telegram/capabilities.py"
      via: "turns the recommended exact-target capability lane into concrete internal seams"
      pattern: "exact-target|fast path|id/metadata|ambiguity"
    - from: "src/mcp_telegram/cache.py"
      to: "tests/test_capabilities.py"
      via: "proves direct topic access reuses tombstones and metadata rather than bypassing forum recovery"
      pattern: "topic|deleted|inaccessible|metadata"
    - from: "tests/test_tools.py"
      to: "tests/test_capabilities.py"
      via: "keeps fast-path coverage rename-resistant and visible below the adapters"
      pattern: "direct|dialog_id|topic_id|ambiguity"
---

<objective>
Create the exact-target foundation for Phase 17.

Purpose: add the narrowest internal capability support needed for direct read/search workflows when
the caller already knows the exact dialog and, for forum reads, topic target.
Output: capability/cache seams plus contract tests that distinguish exact-target fast paths from
the existing ambiguity-preserving helper flows.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-direct-read-search-workflows/17-RESEARCH.md
@.planning/phases/17-direct-read-search-workflows/17-VALIDATION.md
@.planning/phases/13-implementation-sequencing-decision-memo/13-IMPLEMENTATION-MEMO.md
@src/mcp_telegram/tools.py
@src/mcp_telegram/capabilities.py
@src/mcp_telegram/cache.py
@src/mcp_telegram/formatter.py
@tests/test_tools.py
@tests/test_capabilities.py
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exact-target capability lanes for known dialog and topic identifiers</name>
  <files>src/mcp_telegram/capabilities.py, src/mcp_telegram/cache.py</files>
  <action>
Implement the narrowest internal fast path needed for Phase 17.

Required scope:
- allow the history-read and search capabilities to accept exact dialog identifiers without forcing
  the normal fuzzy-resolution path first
- allow the history-read capability to accept exact topic targeting through cached metadata or a
  bounded topic-by-id refresh path instead of loading the full topic catalog by default
- keep exact-target handling opt-in so name-based calls still follow the current ambiguity-safe
  dialog/topic resolution flow
- preserve the existing topic fetch, stale-anchor refresh, fallback scan, and navigation logic once
  a target is known

Guardrails:
- do not change the public tool schemas yet
- do not reopen Phase 16 navigation token design
- do not weaken ambiguity behavior for fuzzy name inputs
  </action>
  <verify>
    <automated>uv run pytest tests/test_capabilities.py -k "history or search or direct or topic or navigation" -q</automated>
  </verify>
  <done>The capability layer can execute exact known-target reads and searches without collapsing the existing safe name-based path.</done>
</task>

<task type="auto">
  <name>Task 2: Add contract tests that pin fast-path scope and preserved forum guarantees</name>
  <files>tests/test_capabilities.py, tests/test_tools.py</files>
  <action>
Add tests that make the intended Phase 17 foundation explicit before the public adapters move.

Required coverage:
- exact known dialog targets bypass discovery-oriented setup
- exact known topic targets bypass full topic-catalog resolution where possible
- fuzzy dialog/topic ambiguity is still explicit and action-oriented
- deleted topics, inaccessible topics, `General`, unread pages, and topic-scoped navigation still
  behave exactly like the brownfield path after the fast-path seam is introduced

Prefer capability-level tests where possible so the direct-workflow seam is visible independently of
future public field names.
  </action>
  <verify>
    <automated>uv run pytest tests/test_capabilities.py -k "direct or history or search or topic" -q && uv run pytest tests/test_tools.py -k "list_messages or search_messages or topic or ambiguity or direct" -q</automated>
  </verify>
  <done>The exact-target seam is anchored in tests without weakening topic fidelity or ambiguity handling.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. exact known-target read/search entrypoints exist below the public tool adapters
2. exact-target support is opt-in and does not change the name-based ambiguity path
3. forum-topic tombstones, recovery, and navigation invariants still hold under the new seam
4. the repo is ready for public direct-workflow fields in later Phase 17 plans
</verification>

<success_criteria>
- [ ] `FLOW-01` has an internal fast-path foundation instead of only docstring guidance
- [ ] `FLOW-02` can reuse the same exact-target seam rather than inventing a search-only shortcut
- [ ] Topic-fidelity and ambiguity guarantees remain explicitly covered in tests
- [ ] No helper-surface posture or Phase 18 classification work enters the plan
- [ ] The public adapters are ready to stay thin in later Phase 17 plans
</success_criteria>

<output>
After completion, create `.planning/phases/17-direct-read-search-workflows/17-01-SUMMARY.md` with:
- exact-target capability seams introduced
- preserved topic and ambiguity invariants
- capability/tool tests added or updated
- quick-test command used
</output>
