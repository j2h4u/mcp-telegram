---
phase: 18-surface-posture-rollout-proof
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - .planning/phases/18-surface-posture-rollout-proof/18-UAT.md
autonomous: true
requirements:
  - ROLL-01
  - ROLL-02

must_haves:
  truths:
    - "The rebuilt and restarted live runtime exposes the same posture-aware contract as the local repository reflection"
    - "Representative primary and secondary tool calls succeed against the long-lived container after restart"
    - "Privacy-safe telemetry proof is rerun alongside rollout proof rather than assumed from earlier waves"
  artifacts:
    - path: ".planning/phases/18-surface-posture-rollout-proof/18-UAT.md"
      provides: "Recorded runtime parity evidence and final rollout sign-off inputs"
      exports: ["Local reflection baseline", "In-container reflection output", "Representative live-call results", "Pass/fail notes"]
    - path: "/opt/docker/mcp-telegram/docker-compose.yml"
      provides: "Documented runtime rebuild/restart path for the live container"
      exports: ["Source refresh discipline", "Long-lived container restart", "Deployment parity step"]
    - path: "tests/test_analytics.py"
      provides: "Runtime-adjacent privacy proof rerun at phase close"
      exports: ["Telemetry schema remains bounded", "No privacy regression before sign-off"]
    - path: "tests/privacy_audit.sh"
      provides: "Static privacy gate rerun at rollout close"
      exports: ["No content logging", "No selector leakage", "No PII field expansion"]
  key_links:
    - from: ".planning/phases/18-surface-posture-rollout-proof/18-UAT.md"
      to: "/opt/docker/mcp-telegram/docker-compose.yml"
      via: "ties the recorded runtime proof to the exact rebuild/restart path used on this machine"
      pattern: "docker compose|mcp-telegram|runtime"
    - from: "17-UAT.md"
      to: ".planning/phases/18-surface-posture-rollout-proof/18-UAT.md"
      via: "reuses known working runtime call patterns for primary workflows instead of inventing new live probes"
      pattern: "exact_dialog_id|SearchMessages|list-tools"
    - from: "tests/test_analytics.py"
      to: "tests/privacy_audit.sh"
      via: "keeps dynamic and static privacy proof in the same final rollout loop"
      pattern: "TelemetryEvent|privacy|tool_name"
---

<objective>
Rebuild and restart the live runtime, then close the phase only after proving that the serving
container exposes the same posture-aware contract and privacy-safe guarantees as the local repo.

Purpose: satisfy the runtime half of `ROLL-01` and confirm `ROLL-02` at rollout close rather than
assuming earlier repo-local proof is enough.
Output: rebuilt-container proof recorded in `18-UAT.md`, with reflected-surface parity and
representative live-call evidence.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-surface-posture-rollout-proof/18-RESEARCH.md
@.planning/phases/18-surface-posture-rollout-proof/18-VALIDATION.md
@.planning/phases/18-surface-posture-rollout-proof/18-02-PLAN.md
@.planning/phases/18-surface-posture-rollout-proof/18-UAT.md
@.planning/phases/17-direct-read-search-workflows/17-UAT.md
@src/mcp_telegram/tools.py
@src/mcp_telegram/server.py
@src/mcp_telegram/analytics.py
@cli.py
@tests/test_analytics.py
@tests/privacy_audit.sh
@devtools/mcp_client/README.md
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the local baseline and rebuild the long-lived runtime</name>
  <files>.planning/phases/18-surface-posture-rollout-proof/18-UAT.md</files>
  <action>
Before touching the live container, capture the final local reflection baseline, then rebuild and
restart the runtime using the documented machine-specific path from `AGENTS.md`.

Required proof:
- record the local `cli.py list-tools` posture-aware contract in `18-UAT.md`
- rebuild and restart `mcp-telegram` with the documented compose file
- treat any mismatch between repo-local reflection and runtime reflection as a phase-closing
  blocker, not a note for later
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run cli.py list-tools && docker compose -f /opt/docker/mcp-telegram/docker-compose.yml up -d --build mcp-telegram</automated>
  </verify>
  <done>The local baseline is recorded and the long-lived runtime has been refreshed from current source.</done>
</task>

<task type="auto">
  <name>Task 2: Prove in-container reflection parity and representative live behavior</name>
  <files>.planning/phases/18-surface-posture-rollout-proof/18-UAT.md</files>
  <action>
Verify that the restarted container is actually serving the intended contract and not a stale
pre-restart snapshot.

Required proof:
- inspect reflected descriptions and schemas inside the container for all seven tools
- confirm representative primary calls still work through the live MCP process
- confirm at least one secondary/operator call still works through the live MCP process
- reuse the known working Phase 17 live targets where possible instead of inventing new runtime
  fixtures during rollout

Suggested representative calls:
- `GetMyAccount` for a stable secondary/operator surface
- `ListMessages` with the known direct dialog id from Phase 17 runtime validation
- `SearchMessages` with the known signed numeric dialog id from Phase 17 runtime validation
  </action>
  <verify>
    <automated>docker exec mcp-telegram /opt/venv/bin/python -c "import json; from mcp_telegram.tools import GetMyAccount, GetUsageStats, GetUserInfo, ListDialogs, ListMessages, ListTopics, SearchMessages, tool_description; tool_types = {'GetMyAccount': GetMyAccount, 'GetUsageStats': GetUsageStats, 'GetUserInfo': GetUserInfo, 'ListDialogs': ListDialogs, 'ListMessages': ListMessages, 'ListTopics': ListTopics, 'SearchMessages': SearchMessages}; print(json.dumps({name: {'description': tool_description(tool_types[name]).description, 'schema': tool_description(tool_types[name]).inputSchema} for name in sorted(tool_types)}, ensure_ascii=True, sort_keys=True))" && UV_CACHE_DIR=/tmp/.uv-cache uv run python -m devtools.mcp_client.cli call-tool --name GetMyAccount --arguments '{}' -- docker exec -i mcp-telegram mcp-telegram run && UV_CACHE_DIR=/tmp/.uv-cache uv run python -m devtools.mcp_client.cli call-tool --name ListMessages --arguments '{"exact_dialog_id":-1003779402801,"limit":2}' -- docker exec -i mcp-telegram mcp-telegram run && UV_CACHE_DIR=/tmp/.uv-cache uv run python -m devtools.mcp_client.cli call-tool --name SearchMessages --arguments '{"dialog":"-1003779402801","query":"MCP","limit":2}' -- docker exec -i mcp-telegram mcp-telegram run</automated>
  </verify>
  <done>The restarted runtime exposes the intended contract and passes representative primary and secondary live calls.</done>
</task>

<task type="auto">
  <name>Task 3: Rerun privacy gates and record final rollout sign-off evidence</name>
  <files>.planning/phases/18-surface-posture-rollout-proof/18-UAT.md</files>
  <action>
Close the phase only after privacy-safe telemetry proof has been rerun alongside the runtime proof.

Required scope:
- rerun `tests/test_analytics.py` and `tests/privacy_audit.sh`
- record pass/fail outcomes in `18-UAT.md`
- include any runtime mismatch or privacy regression as a blocking issue rather than a follow-up
  idea
  </action>
  <verify>
    <automated>uv run pytest tests/test_analytics.py -q && bash tests/privacy_audit.sh</automated>
  </verify>
  <done>The final runtime proof and privacy proof are recorded together as the phase-closing evidence set.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. local reflection and in-container reflection match for the affected tools
2. representative primary and secondary live MCP calls succeed after restart
3. privacy tests are rerun at rollout close, not inherited on trust
4. `18-UAT.md` contains enough evidence for a maintainer to prove the restarted runtime is current
</verification>

<success_criteria>
- [ ] The long-lived runtime is rebuilt and restarted from current source
- [ ] In-container reflection matches the repo-local posture-aware contract
- [ ] Representative primary and secondary live calls succeed after restart
- [ ] `ROLL-01` and `ROLL-02` have recorded rollout-close evidence, not just repo-local proof
</success_criteria>

<output>
After completion, create `.planning/phases/18-surface-posture-rollout-proof/18-03-SUMMARY.md` with:
- rebuild/restart command used
- in-container reflection parity result
- representative live-call results
- final privacy gate status
</output>
