---
phase: 18-surface-posture-rollout-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - tests/test_server.py
  - tests/test_tools.py
  - tests/test_analytics.py
  - tests/privacy_audit.sh
  - .planning/phases/18-surface-posture-rollout-proof/18-UAT.md
autonomous: true
requirements:
  - SURF-01
  - ROLL-01
  - ROLL-02

must_haves:
  truths:
    - "Brownfield behavior tests, local reflection, and the posture artifact all agree on the current public contract"
    - "Telemetry remains privacy-safe after the posture and rollout-proof work"
    - "Repo-local proof is complete before the runtime rebuild step begins"
  artifacts:
    - path: "tests/test_server.py"
      provides: "Reflected-schema and description parity proof for the posture-aware contract"
      exports: ["Surface posture assertions", "Schema visibility checks", "No contract drift between repo and reflection"]
    - path: "tests/test_tools.py"
      provides: "Brownfield behavior proof for primary, secondary/helper, and inspect/operator surfaces"
      exports: ["Direct workflow preservation", "Helper-tool availability", "Per-tool telemetry anchors"]
    - path: "tests/test_analytics.py"
      provides: "Bounded telemetry semantics after posture work"
      exports: ["No identifying payload growth", "Stable telemetry schema", "Posture-era event semantics"]
    - path: "tests/privacy_audit.sh"
      provides: "Static privacy gate for telemetry fields and callsites"
      exports: ["No message content fields", "No identifying selector leakage", "No schema widening without audit updates"]
    - path: ".planning/phases/18-surface-posture-rollout-proof/18-UAT.md"
      provides: "Repo-local and runtime checklist for final rollout proof"
      exports: ["Expected local reflection state", "Expected runtime parity checks", "Known live-call samples to reuse"]
  key_links:
    - from: ".planning/phases/18-surface-posture-rollout-proof/18-SURFACE-POSTURE.md"
      to: "tests/test_server.py"
      via: "turns the posture artifact into executable reflection proof"
      pattern: "primary|secondary|GetUserInfo|ListTopics"
    - from: "tests/test_tools.py"
      to: "tests/test_analytics.py"
      via: "ensures posture work preserves tool behavior and bounded telemetry together"
      pattern: "telemetry|tool_name|privacy|result_count"
    - from: "tests/test_analytics.py"
      to: "tests/privacy_audit.sh"
      via: "keeps dynamic privacy semantics and static field audit aligned"
      pattern: "TelemetryEvent|tool_name|error_type|message content"
---

<objective>
Turn the posture work into repo-local contract proof by aligning brownfield tests, local reflection,
and privacy-safe telemetry checks before the live runtime is rebuilt.

Purpose: close the repository side of `ROLL-01` and `ROLL-02` so the final runtime step is proving
parity, not discovering unfinished repo work.
Output: updated contract/privacy tests plus a rollout checklist that defines exactly what the final
runtime proof must confirm.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-surface-posture-rollout-proof/18-RESEARCH.md
@.planning/phases/18-surface-posture-rollout-proof/18-VALIDATION.md
@.planning/phases/18-surface-posture-rollout-proof/18-01-PLAN.md
@.planning/phases/18-surface-posture-rollout-proof/18-SURFACE-POSTURE.md
@.planning/phases/17-direct-read-search-workflows/17-UAT.md
@src/mcp_telegram/tools.py
@src/mcp_telegram/server.py
@src/mcp_telegram/analytics.py
@cli.py
@tests/test_server.py
@tests/test_tools.py
@tests/test_analytics.py
@tests/privacy_audit.sh
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend brownfield and reflection tests to prove the posture-aware contract locally</name>
  <files>tests/test_server.py, tests/test_tools.py</files>
  <action>
Update the local contract anchors so posture is not merely described in docs.

Required coverage:
- reflected schemas/descriptions identify the intended primary and secondary/helper surfaces
- primary workflows from Phase 17 remain intact and do not rely on helper-first choreography again
- helper and operator tools remain available and clearly secondary rather than silently removed
- `GetUserInfo` is classified consistently with the final posture choice from Plan 01

Guardrails:
- do not rewrite tool behavior to make tests easier
- do not depend solely on `cli.py list-tools` output snapshots when tighter Python-level assertions
  are clearer
  </action>
  <verify>
    <automated>uv run pytest tests/test_server.py -q && uv run pytest tests/test_tools.py -k "schema or posture or list_messages or search_messages or list_dialogs or list_topics or get_user_info" -q</automated>
  </verify>
  <done>Repo-local contract tests now prove the posture-aware public surface before runtime rebuild work starts.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh telemetry and privacy gates for the final Phase 18 contract</name>
  <files>tests/test_analytics.py, tests/privacy_audit.sh</files>
  <action>
Prove that the posture and rollout-proof work does not widen telemetry or logging.

Required scope:
- keep telemetry schema bounded to privacy-safe fields already accepted by the repo
- add or adjust tests if posture markers change which tools should appear as primary versus
  secondary in analytics summaries or expectations
- keep the static audit authoritative for disallowing message content, query payloads, navigation
  payloads, and identifying selector leakage

Guardrails:
- do not add telemetry fields merely to make posture easier to observe
- do not weaken the static audit to accommodate incidental logging
  </action>
  <verify>
    <automated>uv run pytest tests/test_analytics.py -q && bash tests/privacy_audit.sh</automated>
  </verify>
  <done>The final Phase 18 contract remains telemetry-safe and passes the repo privacy gate.</done>
</task>

<task type="auto">
  <name>Task 3: Prepare the rollout checklist and run the local reflection baseline</name>
  <files>.planning/phases/18-surface-posture-rollout-proof/18-UAT.md</files>
  <action>
Write the Phase 18 runtime checklist so the final rollout proof is reproducible instead of ad hoc.

Required contents:
- the exact local reflection expectation from the finished repo
- which representative primary and secondary calls should be rerun against the restarted container
- how to compare local reflection against in-container reflection
- what counts as a failure versus a pass for `ROLL-01` and `ROLL-02`

Then capture the local reflection baseline with `cli.py list-tools`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run cli.py list-tools</automated>
  </verify>
  <done>The repo now has an explicit rollout checklist and a local reflected-surface baseline.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. repo-local reflection and brownfield tests agree on the posture-aware contract
2. the privacy audit and analytics tests stay green after posture work
3. the runtime checklist is concrete enough to execute without rediscovering targets or commands
4. all repo-local proof is complete before the container rebuild step begins
</verification>

<success_criteria>
- [ ] `ROLL-01` has complete repo-local proof before runtime rebuild
- [ ] `ROLL-02` remains green through analytics tests and privacy audit
- [ ] The posture artifact, local reflection, and brownfield tests all say the same thing
- [ ] The final runtime verification step has a concrete checklist to execute
</success_criteria>

<output>
After completion, create `.planning/phases/18-surface-posture-rollout-proof/18-02-SUMMARY.md` with:
- repo-local contract parity proof added
- privacy and telemetry gates refreshed
- local reflection baseline captured
- the runtime/UAT checklist created
</output>
