---
phase: 18-surface-posture-rollout-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_telegram/tools.py
  - tests/test_server.py
  - tests/test_tools.py
  - .planning/phases/18-surface-posture-rollout-proof/18-SURFACE-POSTURE.md
autonomous: true
requirements:
  - SURF-01

must_haves:
  truths:
    - "Maintainers can point to one canonical artifact that classifies every current tool consistently as primary or secondary/helper"
    - "The reflected MCP surface teaches the same posture as the planning artifact without reopening Phase 17 workflow behavior"
    - "Posture drift between planning docs, code, and tests becomes a visible regression"
  artifacts:
    - path: "src/mcp_telegram/tools.py"
      provides: "Bounded code-level source of truth for tool posture and reflected teaching text"
      exports: ["Stable primary versus secondary/helper wording", "No new workflow redesign", "Visible posture markers at the public tool boundary"]
    - path: "tests/test_server.py"
      provides: "Reflection proof that the server-exposed surface matches the intended posture"
      exports: ["Description or schema posture assertions", "Primary/secondary contract anchors", "No silent reflection drift"]
    - path: "tests/test_tools.py"
      provides: "Brownfield guard that posture work did not regress existing tool behavior"
      exports: ["Behavior-preservation checks", "Description/posture anchors where practical"]
    - path: ".planning/phases/18-surface-posture-rollout-proof/18-SURFACE-POSTURE.md"
      provides: "Canonical maintainer-facing posture matrix for the current seven-tool surface"
      exports: ["Classification for every tool", "Rationale", "Evidence links"]
  key_links:
    - from: "13-IMPLEMENTATION-FRAME.md"
      to: ".planning/phases/18-surface-posture-rollout-proof/18-SURFACE-POSTURE.md"
      via: "freezes the Phase 13 role inventory into the current Medium-era source of truth"
      pattern: "primary|secondary|GetUserInfo|ListDialogs|ListTopics"
    - from: "src/mcp_telegram/tools.py"
      to: "tests/test_server.py"
      via: "proves posture markers are actually visible in the reflected MCP surface"
      pattern: "tool_description|description|primary|secondary"
    - from: "17-03-SUMMARY.md"
      to: "tests/test_tools.py"
      via: "keeps the already-landed direct read/search primary workflows intact while posture becomes explicit"
      pattern: "direct|ListMessages|SearchMessages|schema"
---

<objective>
Freeze and expose the current tool-surface posture so maintainers can point to one consistent
primary-versus-secondary/helper story across planning docs, code, and reflected tests.

Purpose: satisfy `SURF-01` without reopening the workflow-shaping work completed in Phases 16-17.
Output: one canonical posture artifact, one bounded code-level posture source of truth, and
reflection-safe tests that make drift visible.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-surface-posture-rollout-proof/18-RESEARCH.md
@.planning/phases/18-surface-posture-rollout-proof/18-VALIDATION.md
@.planning/phases/13-implementation-sequencing-decision-memo/13-IMPLEMENTATION-FRAME.md
@.planning/phases/13-implementation-sequencing-decision-memo/13-IMPLEMENTATION-MEMO.md
@.planning/phases/17-direct-read-search-workflows/17-03-SUMMARY.md
@src/mcp_telegram/tools.py
@src/mcp_telegram/server.py
@tests/test_server.py
@tests/test_tools.py
@cli.py
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one bounded posture source of truth at the public tool boundary</name>
  <files>src/mcp_telegram/tools.py</files>
  <action>
Make the current Medium-era posture explicit at the same boundary that already defines the MCP
surface.

Required scope:
- encode the current posture for all seven tools in one bounded place near the public tool
  definitions or `tool_description()` path
- preserve the Phase 13 baseline that treats `ListMessages`, `SearchMessages`, and `GetUserInfo`
  as primary user-task surfaces
- classify `ListDialogs`, `ListTopics`, `GetMyAccount`, and `GetUsageStats` as secondary/helper or
  operator surfaces without hiding them
- keep the change posture-oriented rather than behavioral

Guardrails:
- do not redesign tool inputs or output format again
- do not add speculative future-removal mechanics
- do not split posture into multiple parallel code paths
  </action>
  <verify>
    <automated>uv run pytest tests/test_server.py -k "list_messages or search_messages or get_user_info or posture or description" -q</automated>
  </verify>
  <done>The reflected public boundary now has one stable code-level posture source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Add reflection and brownfield tests that pin the posture contract</name>
  <files>tests/test_server.py, tests/test_tools.py</files>
  <action>
Add focused assertions that make posture drift a test failure while keeping existing tool behavior
anchored.

Required coverage:
- `tests/test_server.py` should prove the reflected MCP surface teaches the intended posture for
  primary and secondary/helper tools
- `tests/test_tools.py` should keep direct read/search behavior, helper-tool availability, and
  inspect/operator tools green after the posture markers land
- where wording matters, assert the minimum stable phrases rather than brittle full-text matches

Guardrails:
- do not replace behavior coverage with description-only checks
- do not let posture assertions become so strict that harmless wording cleanup causes churn
  </action>
  <verify>
    <automated>uv run pytest tests/test_server.py -q && uv run pytest tests/test_tools.py -k "list_messages or search_messages or list_dialogs or list_topics or get_my_account or get_user_info or get_usage_stats or schema or posture" -q</automated>
  </verify>
  <done>The repo fails fast if posture drifts across reflection and behavior anchors.</done>
</task>

<task type="auto">
  <name>Task 3: Write the canonical Phase 18 posture artifact and cross-check it through local reflection</name>
  <files>.planning/phases/18-surface-posture-rollout-proof/18-SURFACE-POSTURE.md</files>
  <action>
Create the maintainer-facing artifact that records the final current-tool classification and links
back to the evidence that proves it.

Required contents:
- one row per current tool
- classification (`primary` or `secondary/helper`) plus a short rationale
- evidence links to code/tests or prior phase artifacts
- a short note that this is the current Medium-era posture, not a speculative future-removal plan

Then run local reflection to confirm the planning artifact and the surfaced tool contract still
agree.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run cli.py list-tools</automated>
  </verify>
  <done>Maintainers now have one phase-local artifact they can cite first when explaining the current surface posture.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. every current tool has one explicit primary versus secondary/helper classification
2. the Phase 13 posture baseline is preserved rather than reinvented
3. local reflection exposes the intended posture teaching surface
4. existing read/search/helper/account behavior still passes brownfield tests
</verification>

<success_criteria>
- [ ] `SURF-01` is anchored in planning docs, code, and tests
- [ ] The current seven-tool surface has one consistent posture vocabulary
- [ ] Local reflection and planning artifacts agree on the intended posture
- [ ] No new workflow-shape redesign is introduced while landing posture markers
</success_criteria>

<output>
After completion, create `.planning/phases/18-surface-posture-rollout-proof/18-01-SUMMARY.md` with:
- the final tool posture source of truth
- reflection and brownfield tests added
- the canonical posture artifact created
- local reflection result noted
</output>
