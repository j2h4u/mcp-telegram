---
phase: 16-unified-navigation-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/mcp_telegram/tools.py
  - src/mcp_telegram/capabilities.py
  - src/mcp_telegram/analytics.py
  - tests/test_tools.py
  - tests/test_server.py
  - tests/test_analytics.py
autonomous: true
requirements:
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "`SearchMessages` now uses the same continuation vocabulary as `ListMessages`"
    - "Hit-local search context remains intact under the unified contract"
    - "Local reflection and restarted-runtime checks agree on the final Phase 16 public schema"
  artifacts:
    - path: "src/mcp_telegram/tools.py"
      provides: "Final unified navigation surface across read and search tools"
      exports: ["Shared search navigation input", "Shared continuation footer", "Removed split vocabulary"]
    - path: "tests/test_tools.py"
      provides: "Search regression coverage under the unified contract"
      exports: ["Schema assertions", "Context-window assertions", "Continuation assertions"]
    - path: "tests/test_server.py"
      provides: "Reflection and MCP-boundary proof for the final Phase 16 surface"
      exports: ["Final tool schema visibility", "ToolArgs validation coverage"]
    - path: "tests/test_analytics.py"
      provides: "Bounded telemetry verification for the navigation migration"
      exports: ["Privacy-safe telemetry assertions", "Navigation-related event semantics"]
  key_links:
    - from: "16-RESEARCH.md"
      to: "src/mcp_telegram/tools.py"
      via: "removes the last public split term and closes NAV-01"
      pattern: "SearchMessages|offset|shared continuation vocabulary|runtime freshness"
    - from: "src/mcp_telegram/capabilities.py"
      to: "tests/test_tools.py"
      via: "preserves hit-local context while moving search continuation to the shared contract"
      pattern: "search|context|offset|navigation"
    - from: "src/mcp_telegram/tools.py"
      to: "tests/test_analytics.py"
      via: "keeps telemetry bounded and privacy-safe through the public contract migration"
      pattern: "SearchMessages|has_cursor|telemetry|privacy"
---

<objective>
Finish Phase 16 by migrating `SearchMessages` to the shared navigation contract and proving the
changed surface in both repo and live runtime.

Purpose: remove the remaining split continuation vocabulary, preserve search fidelity, and complete
the reflection/runtime freshness proof required for public schema changes.
Output: final unified navigation surface, regression coverage, telemetry proof, and restarted
runtime verification.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-unified-navigation-contract/16-RESEARCH.md
@.planning/phases/16-unified-navigation-contract/16-VALIDATION.md
@.planning/phases/16-unified-navigation-contract/16-02-PLAN.md
@src/mcp_telegram/tools.py
@src/mcp_telegram/capabilities.py
@src/mcp_telegram/pagination.py
@src/mcp_telegram/server.py
@src/mcp_telegram/analytics.py
@cli.py
@tests/test_tools.py
@tests/test_server.py
@tests/test_analytics.py
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move `SearchMessages` to the shared navigation vocabulary</name>
  <files>src/mcp_telegram/tools.py, src/mcp_telegram/capabilities.py</files>
  <action>
Update `SearchMessages` to use the same continuation vocabulary already adopted by `ListMessages`.

Required scope:
- retire `offset` and `next_offset` at the public surface in favor of the shared navigation fields
- preserve correct Telegram search pagination internally
- keep hit-local context, dialog resolution, and no-hit/action-oriented behavior intact
- ensure search token validation rejects mismatched dialog/query reuse cleanly

Guardrails:
- do not reshape helper-first workflows yet
- do not broaden logging or telemetry payloads
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "search_messages or offset or navigation" -q && uv run pytest tests/test_capabilities.py -k "search" -q</automated>
  </verify>
  <done>`SearchMessages` now speaks the shared navigation vocabulary without losing search fidelity.</done>
</task>

<task type="auto">
  <name>Task 2: Update reflection and telemetry coverage for the final Phase 16 surface</name>
  <files>tests/test_tools.py, tests/test_server.py, tests/test_analytics.py, src/mcp_telegram/analytics.py</files>
  <action>
Add the final contract and bounded-telemetry proof for Phase 16.

Required coverage:
- reflected schemas for `ListMessages` and `SearchMessages` now teach the same continuation model
- the old split vocabulary is no longer required by schema or footer assertions
- telemetry remains privacy-safe and any navigation-related field semantics are either updated or
  explicitly kept bounded without leaking content

Prefer targeted analytics updates over a broad telemetry redesign.
  </action>
  <verify>
    <automated>uv run pytest tests/test_server.py tests/test_analytics.py -q && uv run pytest tests/test_tools.py -k "schema or list_messages or search_messages or telemetry" -q && uv run cli.py list-tools</automated>
  </verify>
  <done>The final Phase 16 public schema and bounded telemetry semantics are proven locally.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild and restart the runtime to prove the unified contract is live</name>
  <files>src/mcp_telegram/tools.py, src/mcp_telegram/capabilities.py</files>
  <action>
Because this plan changes reflected public tool schemas, update the long-lived runtime on this
machine instead of relying on repo-local tests alone.

Required proof:
- capture local reflected tool schemas with `uv run cli.py list-tools`
- rebuild and restart `mcp-telegram` using the documented compose file
- verify inside the container that the updated code imports successfully
- confirm the restarted runtime exposes the intended final schemas for `ListMessages` and
  `SearchMessages`
  </action>
  <verify>
    <automated>uv run cli.py list-tools && docker compose -f /opt/docker/mcp-telegram/docker-compose.yml up -d --build mcp-telegram && docker exec mcp-telegram /opt/venv/bin/python -c "import json; from mcp_telegram.tools import ListMessages, SearchMessages, tool_description; print(json.dumps({'ListMessages': tool_description(ListMessages).inputSchema, 'SearchMessages': tool_description(SearchMessages).inputSchema}, ensure_ascii=True, sort_keys=True))"</automated>
  </verify>
  <done>The restarted runtime is confirmed to expose the unified Phase 16 navigation contract.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. both tools expose one shared continuation vocabulary in reflected schemas and output footers
2. search still returns hit-local context correctly under the new contract
3. malformed or mismatched navigation state is rejected cleanly
4. telemetry remains privacy-safe after the migration
5. the rebuilt and restarted runtime exposes the same intended schema as local reflection
</verification>

<success_criteria>
- [ ] `NAV-01` is fully satisfied across both tools
- [ ] `NAV-02` search-side fidelity remains intact
- [ ] Local reflection and live runtime agree on the final Phase 16 contract
- [ ] Privacy-safe telemetry guarantees remain preserved
- [ ] The repo is ready for Phase 17 workflow-shape work on top of the unified navigation layer
</success_criteria>

<output>
After completion, create `.planning/phases/16-unified-navigation-contract/16-03-SUMMARY.md` with:
- final unified navigation surface landed
- preserved search/context guarantees
- telemetry/reflection checks performed
- runtime rebuild/restart verification used
</output>
