---
phase: 16-unified-navigation-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_telegram/pagination.py
  - src/mcp_telegram/capabilities.py
  - tests/test_capabilities.py
  - tests/test_tools.py
autonomous: true
requirements:
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "Phase 16 defines one shared navigation primitive before public tool adapters are migrated"
    - "Read and search continuation state can be represented through one inspectable internal contract"
    - "Malformed or mismatched navigation state has explicit failure coverage before schema migration begins"
  artifacts:
    - path: "src/mcp_telegram/pagination.py"
      provides: "Shared navigation-token encode/decode behavior for history and search flows"
      exports: ["Opaque token helpers", "Mismatch protection inputs", "Bounded failure surface"]
    - path: "src/mcp_telegram/capabilities.py"
      provides: "Capability-level navigation contract shared by history reads and searches"
      exports: ["Shared navigation result shape", "History/search token translation", "Action-oriented token failures"]
    - path: "tests/test_capabilities.py"
      provides: "Capability-level anchors for the unified navigation primitive"
      exports: ["History continuation tests", "Search continuation tests", "Mismatch and invalid-token tests"]
  key_links:
    - from: "16-RESEARCH.md"
      to: "src/mcp_telegram/pagination.py"
      via: "turns the recommended opaque shared navigation contract into concrete primitives"
      pattern: "shared navigation|opaque token|dialog|query|mismatch"
    - from: "src/mcp_telegram/capabilities.py"
      to: "tests/test_capabilities.py"
      via: "proves read/search capability outputs now converge on one navigation model"
      pattern: "HistoryReadExecution|SearchExecution|navigation|cursor|offset"
    - from: "tests/test_tools.py"
      to: "tests/test_capabilities.py"
      via: "lifts split-vocabulary coverage into rename-resistant contract anchors"
      pattern: "next_cursor|next_offset|from_beginning|offset|topic"
---

<objective>
Create the internal navigation foundation for Phase 16.

Purpose: introduce the narrowest shared navigation primitive that can represent both history-read
and search continuation state before the public tool schemas change.
Output: updated pagination/capability internals plus seam-focused tests that anchor the unified
contract and its mismatch protections.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-unified-navigation-contract/16-RESEARCH.md
@.planning/phases/16-unified-navigation-contract/16-VALIDATION.md
@.planning/phases/13-implementation-sequencing-decision-memo/13-IMPLEMENTATION-MEMO.md
@src/mcp_telegram/tools.py
@src/mcp_telegram/capabilities.py
@src/mcp_telegram/pagination.py
@src/mcp_telegram/server.py
@tests/test_tools.py
@tests/test_capabilities.py
@tests/test_pagination.py
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define one shared navigation primitive across history and search capabilities</name>
  <files>src/mcp_telegram/pagination.py, src/mcp_telegram/capabilities.py</files>
  <action>
Implement the narrowest shared internal navigation contract needed by Phase 16.

Required scope:
- represent both history-read continuation and search continuation through one opaque token family
- include enough state to reject mismatched reuse safely; history must preserve dialog scoping and
  search should gain equivalent dialog/query mismatch protection
- keep Telethon paging details (`min_id`, `max_id`, `reverse`, `add_offset`) internal to the
  capability layer
- expose one shared capability-level result/output shape that later plans can surface publicly

Guardrails:
- do not change public tool names yet
- do not do Phase 17 helper-flow simplification work
- keep topic-aware pagination semantics intact for history reads
  </action>
  <verify>
    <automated>uv run pytest tests/test_capabilities.py -k "history or search or cursor or offset or navigation" -q</automated>
  </verify>
  <done>The repo has one inspectable internal navigation primitive for both read and search flows.</done>
</task>

<task type="auto">
  <name>Task 2: Add contract anchors for invalid and mismatched navigation reuse</name>
  <files>tests/test_capabilities.py, tests/test_tools.py</files>
  <action>
Add tests that make the intended Phase 16 contract explicit before the public adapters migrate.

Required coverage:
- valid history continuation under the shared primitive
- valid search continuation under the shared primitive
- invalid token failures with action-oriented guidance
- mismatched dialog/query/tool reuse rejection
- preservation of topic-scoped continuation behavior while the shared primitive is introduced

Prefer direct capability-level tests where possible so the contract is visible independently of the
public tool names.
  </action>
  <verify>
    <automated>uv run pytest tests/test_capabilities.py -k "history or search or cursor or offset or navigation" -q && uv run pytest tests/test_tools.py -k "list_messages or search_messages or cursor or offset or topic" -q</automated>
  </verify>
  <done>The unified navigation primitive is anchored in tests before schema migration begins.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. one shared opaque navigation primitive exists below the tool adapters
2. history and search capabilities can both derive continuation state from that primitive
3. invalid and mismatched tokens return explicit failures
4. topic-scoped pagination invariants remain covered
5. the repo is ready for public-schema migration in later Phase 16 plans
</verification>

<success_criteria>
- [ ] `NAV-01` has a concrete internal foundation instead of only renamed prose
- [ ] Search continuation is hardened beyond raw integer offset reuse
- [ ] Topic-aware read pagination remains preserved at the capability level
- [ ] No Phase 17 workflow-shape scope enters the plan
- [ ] The repo has clear tests for invalid and mismatched continuation state
</success_criteria>

<output>
After completion, create `.planning/phases/16-unified-navigation-contract/16-01-SUMMARY.md` with:
- shared navigation primitive introduced
- mismatch protections added
- capability-level tests added or updated
- quick-test command used
</output>
