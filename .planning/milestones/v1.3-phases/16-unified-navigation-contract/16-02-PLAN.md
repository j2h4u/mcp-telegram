---
phase: 16-unified-navigation-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/mcp_telegram/tools.py
  - src/mcp_telegram/capabilities.py
  - tests/test_tools.py
  - tests/test_capabilities.py
  - tests/test_server.py
autonomous: true
requirements:
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "`ListMessages` teaches the new shared navigation vocabulary instead of separate `cursor` and `from_beginning` concepts"
    - "Topic-scoped reads, sender filtering, ambiguity handling, and transcript readability survive the contract change"
    - "Local reflection proves the intended `ListMessages` schema after migration"
  artifacts:
    - path: "src/mcp_telegram/tools.py"
      provides: "Unified `ListMessages` public contract and footer wording"
      exports: ["Shared navigation input", "Shared continuation footer", "Preserved read/topic user guidance"]
    - path: "src/mcp_telegram/capabilities.py"
      provides: "History-read execution updated to consume and emit the shared navigation model"
      exports: ["First-page direction handling", "Topic-aware continuation", "Action-oriented token failures"]
    - path: "tests/test_tools.py"
      provides: "`ListMessages` regression coverage under the unified contract"
      exports: ["Schema assertions", "Topic read assertions", "Ambiguity and deleted/inaccessible-topic assertions"]
  key_links:
    - from: "16-RESEARCH.md"
      to: "src/mcp_telegram/tools.py"
      via: "moves read-side public vocabulary to the shared contract without reshaping workflows"
      pattern: "ListMessages|shared continuation vocabulary|from_beginning|topic fidelity"
    - from: "src/mcp_telegram/capabilities.py"
      to: "tests/test_tools.py"
      via: "preserves topic-aware pagination and action-oriented failures while the surface changes"
      pattern: "topic|deleted|inaccessible|ambiguous|navigation"
    - from: "src/mcp_telegram/server.py"
      to: "tests/test_server.py"
      via: "proves reflected schema changes are visible to the MCP server boundary"
      pattern: "tool_description|tool_args|ListMessages"
---

<objective>
Migrate `ListMessages` to the unified Phase 16 navigation contract.

Purpose: replace the split read-side continuation vocabulary with the shared navigation model while
preserving topic fidelity, ambiguity behavior, and readable transcript output.
Output: updated `ListMessages` schema/adapter/capability flow plus regression coverage and local
reflection proof.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-unified-navigation-contract/16-RESEARCH.md
@.planning/phases/16-unified-navigation-contract/16-VALIDATION.md
@.planning/phases/16-unified-navigation-contract/16-01-PLAN.md
@src/mcp_telegram/tools.py
@src/mcp_telegram/capabilities.py
@src/mcp_telegram/pagination.py
@src/mcp_telegram/formatter.py
@src/mcp_telegram/server.py
@tests/test_tools.py
@tests/test_capabilities.py
@tests/test_server.py
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace `ListMessages` split navigation terms with the shared vocabulary</name>
  <files>src/mcp_telegram/tools.py, src/mcp_telegram/capabilities.py</files>
  <action>
Update the public `ListMessages` contract to use the shared Phase 16 navigation vocabulary.

Required scope:
- retire the old read-side split terms (`cursor`, `next_cursor`, `from_beginning`) at the public
  surface in favor of the chosen shared navigation fields/footer wording
- keep newest-first and oldest-first history entry points available through that same unified
  navigation family rather than a separate boolean concept
- preserve all current dialog resolution, sender filtering, topic resolution, tombstone, and
  inaccessible-topic behavior
- keep transcript readability and existing `[topic: ...]`, `[resolved: ...]`, and `Action:` cues
  coherent after the footer change

Guardrails:
- do not simplify helper-first workflows yet
- do not merge read and search execution paths beyond shared navigation usage
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "list_messages or from_beginning or cursor or topic or sender" -q && uv run pytest tests/test_capabilities.py -k "history or topic" -q</automated>
  </verify>
  <done>`ListMessages` now exposes the shared navigation contract without losing read/topic fidelity.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the reflected `ListMessages` schema and regression surface locally</name>
  <files>tests/test_tools.py, tests/test_server.py</files>
  <action>
Update reflection/contract coverage so the MCP-facing `ListMessages` schema is pinned explicitly.

Required coverage:
- reflected schema shows the new shared navigation fields and no longer teaches the old split
  vocabulary
- topic-scoped pagination, deleted-topic, inaccessible-topic, and ambiguity flows still pass
- malformed or mismatched navigation state returns action-oriented failures

Use local reflection via tests or `cli.py list-tools` checks where that gives the clearest proof.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "schema or list_messages or topic or ambiguous or deleted or inaccessible" -q && uv run pytest tests/test_server.py -q && uv run cli.py list-tools</automated>
  </verify>
  <done>The repo proves the intended `ListMessages` schema and behavior locally after the migration.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. `ListMessages` no longer teaches separate `cursor` / `from_beginning` concepts
2. topic-scoped reads still preserve deletion, inaccessibility, ambiguity, and leak-filtering behavior
3. malformed navigation state stays action-oriented
4. local reflection shows the intended new `ListMessages` schema
5. the repo is ready to migrate `SearchMessages` onto the same public vocabulary
</verification>

<success_criteria>
- [ ] `ListMessages` exposes the shared navigation model clearly
- [ ] `NAV-02` read-side fidelity is preserved under the new contract
- [ ] Reflection and server-boundary coverage pin the changed schema
- [ ] No search-specific workflow reshaping is pulled forward into this plan
</success_criteria>

<output>
After completion, create `.planning/phases/16-unified-navigation-contract/16-02-SUMMARY.md` with:
- `ListMessages` contract changes landed
- preserved topic/read guarantees
- reflection check performed
- quick-test command used
</output>
