---
phase: 15-capability-seams
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/mcp_telegram/tools.py
  - src/mcp_telegram/capabilities.py
  - tests/test_capabilities.py
  - tests/test_tools.py
autonomous: true
requirements:
  - CAP-01

must_haves:
  truths:
    - "`SearchMessages` stops duplicating shared target-resolution and enrichment behavior locally"
    - "Shared read/search/topic behavior can now change through capability-oriented internals without first editing three public tool bodies"
    - "Phase 15 ends with visible seam proof in code and tests while keeping the public search contract stable"
  artifacts:
    - path: "src/mcp_telegram/tools.py"
      provides: "Thin `SearchMessages` adapter over shared capability paths"
      exports: ["Delegating SearchMessages entrypoint", "Reduced tool-local search orchestration"]
    - path: "src/mcp_telegram/capabilities.py"
      provides: "Shared search-compatible targeting and enrichment paths"
      exports: ["Search target resolution", "Shared message enrichment", "Search result-shaping hooks"]
    - path: "tests/test_capabilities.py"
      provides: "Final seam proof for shared read/search/topic behavior"
      exports: ["Search seam tests", "Thin-adapter evidence", "Shared-behavior anchors"]
  key_links:
    - from: "15-02-PLAN.md"
      to: "src/mcp_telegram/tools.py"
      via: "extends the thinned adapter posture from read/topic tools to search"
      pattern: "ListMessages|SearchMessages|shared enrichment|target resolution"
    - from: "15-RESEARCH.md"
      to: "src/mcp_telegram/capabilities.py"
      via: "closes the remaining duplication around search context expansion and enrichment"
      pattern: "SearchMessages seam|reaction-name enrichment|sender cache warmup"
    - from: "tests/test_tools.py"
      to: "tests/test_capabilities.py"
      via: "proves CAP-01 through shared internal seams instead of tool-name-shaped coverage only"
      pattern: "search_messages|offset|reaction|telemetry|capability"
---

<objective>
Migrate `SearchMessages` onto the shared capability seams and finish the Phase 15 seam proof.

Purpose: remove the remaining search-specific duplication that prevents shared read/search/topic
behavior from evolving through one capability-oriented internal layer.
Output: a thinned `SearchMessages` adapter plus final seam-proof tests showing CAP-01 is satisfied.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-capability-seams/15-RESEARCH.md
@.planning/phases/15-capability-seams/15-VALIDATION.md
@.planning/phases/15-capability-seams/15-01-PLAN.md
@.planning/phases/15-capability-seams/15-02-PLAN.md
@src/mcp_telegram/capabilities.py
@src/mcp_telegram/tools.py
@src/mcp_telegram/formatter.py
@tests/test_capabilities.py
@tests/test_tools.py
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire `SearchMessages` to shared target-resolution and enrichment paths</name>
  <files>src/mcp_telegram/tools.py, src/mcp_telegram/capabilities.py</files>
  <action>
Refactor `SearchMessages` so it reuses the shared capability seams introduced earlier instead of
duplicating target resolution and message enrichment logic locally.

Required outcomes:
- search target resolution flows through the same capability-oriented path used by the other dialog
  tools where appropriate
- shared sender cache warmup and reaction-name enrichment are no longer implemented only inside the
  `SearchMessages` body
- search-specific context-window and `[HIT]` marking behavior remains explicit without turning into
  a public contract change
- the existing `offset` contract remains intact during this phase
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "search_messages or offset or reaction" -q</automated>
  </verify>
  <done>`SearchMessages` now rides the shared capability layer instead of duplicating its own core path.</done>
</task>

<task type="auto">
  <name>Task 2: Add final CAP-01 seam proof and run the full regression loop</name>
  <files>tests/test_capabilities.py, tests/test_tools.py</files>
  <action>
Add the final focused tests or structural assertions needed to prove CAP-01 is satisfied:

- maintainers can change shared read/search/topic behavior through the capability layer without
  first editing all three public tool bodies
- public adapters remain thin enough that later navigation and workflow phases can reuse the same
  internals
- existing brownfield search/read/topic behavior and telemetry expectations remain green

Keep this proof practical. Prefer explicit internal tests and targeted public regressions over
meta-programming or brittle line-count heuristics.
  </action>
  <verify>
    <automated>uv run pytest tests/test_capabilities.py -q && uv run pytest tests/test_tools.py -k "list_topics or list_messages or search_messages or topic or cursor or offset or reaction or telemetry" -q && uv run pytest</automated>
  </verify>
  <done>Phase 15 ends with repository-level proof that the capability seams are real and brownfield-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild and restart the runtime, then verify the final Phase 15 surface in-container</name>
  <files>src/mcp_telegram/tools.py, src/mcp_telegram/capabilities.py</files>
  <action>
This final plan changes active runtime code and closes the phase, so it must include the full
runtime-refresh discipline from `AGENTS.md`.

Required proof:
- rebuild and restart `mcp-telegram`
- verify inside the container that the final code imports successfully
- confirm the expected public tools still exist after restart
- where practical, run one reflection or import check inside the container that proves the live
  runtime is serving the latest source rather than a stale pre-restart snapshot
  </action>
  <verify>
    <automated>docker compose -f /opt/docker/mcp-telegram/docker-compose.yml up -d --build mcp-telegram && docker exec mcp-telegram /opt/venv/bin/python -c "import mcp_telegram.capabilities, mcp_telegram.tools as t; print(hasattr(t, 'ListTopics'), hasattr(t, 'ListMessages'), hasattr(t, 'SearchMessages'))"</automated>
  </verify>
  <done>The live runtime is refreshed and verified alongside the final CAP-01 seam proof.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. `SearchMessages` no longer duplicates shared target-resolution and enrichment logic locally
2. CAP-01 is explicitly proven through code structure and tests
3. public `offset` behavior remains stable
4. targeted read/search/topic regressions pass
5. the full pytest suite is green
6. the restarted runtime is current and verified in-container
</verification>

<success_criteria>
- [ ] The last major tool-shaped execution path has been moved onto shared capability seams
- [ ] `CAP-01` is satisfied without changing the public read/search/topic contract prematurely
- [ ] Later navigation and workflow phases can reuse the shared internals introduced here
- [ ] Search-specific fidelity remains preserved after the refactor
- [ ] The restarted runtime is refreshed and verified after the source change
- [ ] Full repository verification still passes after the internal migration
</success_criteria>

<output>
After completion, create `.planning/phases/15-capability-seams/15-03-SUMMARY.md` with:
- search migration changes
- final CAP-01 seam proof
- regression coverage run
- full-suite result
</output>
