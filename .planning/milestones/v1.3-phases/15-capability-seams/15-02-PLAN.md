---
phase: 15-capability-seams
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/mcp_telegram/tools.py
  - src/mcp_telegram/capabilities.py
  - tests/test_capabilities.py
  - tests/test_tools.py
autonomous: true
requirements:
  - CAP-01

must_haves:
  truths:
    - "`ListTopics` and `ListMessages` become thin adapters over shared capability paths instead of owning topic orchestration locally"
    - "Topic-scoped read behavior remains explicit about deleted, inaccessible, stale, and general-topic cases after the refactor"
    - "Shared read/topic behavior is now evolvable through capability code rather than duplicated tool branches"
  artifacts:
    - path: "src/mcp_telegram/tools.py"
      provides: "Thin public adapters for `ListTopics` and `ListMessages`"
      exports: ["Delegating ListTopics entrypoint", "Delegating ListMessages entrypoint", "Reduced tool-local orchestration"]
    - path: "src/mcp_telegram/capabilities.py"
      provides: "Reusable read/topic capability execution paths"
      exports: ["Topic catalog execution", "Topic-scoped history execution", "Shared read/topic enrichment hooks"]
  key_links:
    - from: "15-01-PLAN.md"
      to: "src/mcp_telegram/tools.py"
      via: "consumes the new capability primitives from the foundation plan"
      pattern: "capabilities.py|ListTopics|ListMessages"
    - from: "15-RESEARCH.md"
      to: "src/mcp_telegram/tools.py"
      via: "thins the public adapters while preserving topic recovery and pagination behavior"
      pattern: "forum-topic capability seam|message enrichment|thin adapters"
    - from: "tests/test_tools.py"
      to: "src/mcp_telegram/tools.py"
      via: "proves brownfield topic and read behavior stays stable while orchestration moves inward"
      pattern: "deleted|inaccessible|cursor|from_beginning|unread|topic"
---

<objective>
Migrate the topic and history-read tools onto shared capability seams.

Purpose: make `ListTopics` and `ListMessages` thin public adapters over shared internal read/topic
capability paths.
Output: refactored `src/mcp_telegram/tools.py` and updated capability tests proving preserved topic
and history behavior.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-capability-seams/15-RESEARCH.md
@.planning/phases/15-capability-seams/15-VALIDATION.md
@.planning/phases/15-capability-seams/15-01-PLAN.md
@src/mcp_telegram/capabilities.py
@src/mcp_telegram/tools.py
@src/mcp_telegram/pagination.py
@src/mcp_telegram/formatter.py
@tests/test_capabilities.py
@tests/test_tools.py
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire `ListTopics` and `ListMessages` to delegate to shared capabilities</name>
  <files>src/mcp_telegram/tools.py, src/mcp_telegram/capabilities.py</files>
  <action>
Refactor `ListTopics` and `ListMessages` so they primarily orchestrate input/output adaptation and
delegate the real read/topic behavior to the capability module introduced in Plan 01.

Required outcomes:
- `ListTopics` delegates dialog-target and topic-catalog work through shared capability code
- `ListMessages` delegates dialog-target, topic resolution, topic-scoped fetch/recovery, and shared
  enrichment hooks through capability code
- existing `cursor`, `from_beginning`, `unread`, `sender`, and `topic` public behavior remains
  intact

Keep the refactor Phase 15-bounded:
- do not unify `cursor` and `offset`
- do not make forum reads more direct at the public contract level
- do not merge tool roles
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "list_topics or list_messages or topic or cursor" -q</automated>
  </verify>
  <done>`ListTopics` and `ListMessages` are thin adapters over shared read/topic capability seams.</done>
</task>

<task type="auto">
  <name>Task 2: Prove preserved topic recovery and read-path fidelity after adapter thinning</name>
  <files>tests/test_capabilities.py, tests/test_tools.py</files>
  <action>
Update the focused tests so they prove the refactor preserved the important brownfield behaviors:

- active/general/deleted/inaccessible topic handling
- stale-topic refresh and dialog-scan fallback
- topic-scoped sender/unread filtering
- cursor emission and reverse-pagination behavior
- public action-text recovery when ambiguity or invalid input occurs

Add or refine internal tests where they make the new capability seams clearer than more public
tool assertions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_capabilities.py -q && uv run pytest tests/test_tools.py -k "topic or cursor or unread or from_beginning" -q</automated>
  </verify>
  <done>The read/topic adapter migration is proven against the brownfield fidelity anchors.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh the runtime and verify the thinned read/topic adapters in-container</name>
  <files>src/mcp_telegram/tools.py, src/mcp_telegram/capabilities.py</files>
  <action>
This plan changes active runtime code. Rebuild and restart `mcp-telegram`, then verify inside the
container that the updated read/topic code loads and the public tool surface remains stable.

Required proof:
- rebuild and restart the runtime using the documented compose path
- verify inside the container that `mcp_telegram.tools` imports successfully with the new
  capability wiring
- confirm `ListTopics`, `ListMessages`, and `SearchMessages` are still present after the restart
  </action>
  <verify>
    <automated>docker compose -f /opt/docker/mcp-telegram/docker-compose.yml up -d --build mcp-telegram && docker exec mcp-telegram /opt/venv/bin/python -c "import mcp_telegram.tools as t; print(hasattr(t, 'ListTopics'), hasattr(t, 'ListMessages'), hasattr(t, 'SearchMessages'))"</automated>
  </verify>
  <done>The restarted runtime is current and still exposes the expected read/topic surface.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. `ListTopics` and `ListMessages` visibly delegate to capability code
2. deleted, inaccessible, and stale-topic recovery behavior is preserved
3. `cursor`, `from_beginning`, `unread`, and `sender` behavior still passes brownfield tests
4. internal seam tests remain green
5. the restarted runtime loads the new adapter/capability wiring cleanly
6. the repo is ready for `SearchMessages` migration without another foundation refactor
</verification>

<success_criteria>
- [ ] `ListTopics` and `ListMessages` no longer own most capability logic locally
- [ ] Topic and history-read behavior remains brownfield-stable after the refactor
- [ ] Public tool signatures remain Phase 15-stable
- [ ] The capability module now carries the shared read/topic behavior needed by future phases
- [ ] The restarted runtime is refreshed and verified after the source change
- [ ] The repo has explicit evidence that adapter thinness improved rather than only code motion
</success_criteria>

<output>
After completion, create `.planning/phases/15-capability-seams/15-02-SUMMARY.md` with:
- adapter-thinning changes
- preserved topic/read behaviors
- capability paths now shared
- quick-test command used
</output>
