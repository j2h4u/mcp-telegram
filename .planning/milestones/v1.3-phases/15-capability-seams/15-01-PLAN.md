---
phase: 15-capability-seams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_telegram/capabilities.py
  - tests/test_capabilities.py
  - tests/test_tools.py
autonomous: true
requirements:
  - CAP-01

must_haves:
  truths:
    - "Phase 15 exposes explicit internal capability seams before public-tool adapters are thinned"
    - "Dialog-target and forum-topic behavior can be exercised independently of `ListTopics`, `ListMessages`, and `SearchMessages`"
    - "The new seam contract preserves current public tool schemas and brownfield recovery semantics"
  artifacts:
    - path: "src/mcp_telegram/capabilities.py"
      provides: "Shared capability primitives for dialog targeting and forum-topic behavior"
      exports: ["Dialog-target resolution seam", "Forum-topic capability seam", "Shared capability inputs/outputs"]
    - path: "tests/test_capabilities.py"
      provides: "Internal seam contract coverage independent of public tool names"
      exports: ["Dialog-target capability tests", "Topic capability tests", "Behavior-preservation anchors"]
  key_links:
    - from: "15-RESEARCH.md"
      to: "src/mcp_telegram/capabilities.py"
      via: "turns the identified brownfield primitives into explicit capability seams"
      pattern: "_resolve_dialog|_load_dialog_topics|_fetch_messages_for_topic|capability seam"
    - from: "src/mcp_telegram/tools.py"
      to: "src/mcp_telegram/capabilities.py"
      via: "moves reusable read/topic behavior out of tool-name-shaped orchestration"
      pattern: "ListTopics|ListMessages|SearchMessages|topic|dialog|cursor"
    - from: "tests/test_tools.py"
      to: "tests/test_capabilities.py"
      via: "lifts brownfield behavior clusters into rename-resistant seam coverage"
      pattern: "ambiguous|topic|deleted|inaccessible|cursor|offset"
---

<objective>
Create the explicit Phase 15 capability foundation.

Purpose: introduce bounded internal capability seams for dialog targeting and forum-topic behavior
before the public tool adapters are migrated.
Output: `src/mcp_telegram/capabilities.py` plus seam-focused tests that make the new boundaries
visible independently of public tool names.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-capability-seams/15-RESEARCH.md
@.planning/phases/15-capability-seams/15-VALIDATION.md
@.planning/phases/13-implementation-sequencing-decision-memo/13-IMPLEMENTATION-MEMO.md
@src/mcp_telegram/tools.py
@src/mcp_telegram/cache.py
@src/mcp_telegram/pagination.py
@src/mcp_telegram/formatter.py
@tests/test_tools.py
@tests/test_cache.py
@tests/conftest.py
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce bounded capability primitives for dialog targets and forum topics</name>
  <files>src/mcp_telegram/capabilities.py</files>
  <action>
Create `src/mcp_telegram/capabilities.py` and extract the narrowest shared internal seams needed by
Phase 15.

Required scope:
- one reusable dialog-target path that wraps dialog resolution plus resolved-prefix and actionable
  not-found / ambiguous outcomes for dialog-oriented tools
- one reusable forum-topic path that loads topic catalogs, resolves requested topics, and preserves
  deleted / inaccessible / stale-topic semantics needed by both topic listing and topic-scoped
  reads
- stable internal data structures or return shapes that make those seams inspectable without
  introducing a speculative framework

Guardrails:
- do not change public tool class names or public tool schemas
- do not start unified `cursor` / `offset` contract work
- keep extraction bounded; prefer lifting existing helpers over inventing a large service layer
  </action>
  <verify>
    <automated>uv run pytest tests/test_capabilities.py -q</automated>
  </verify>
  <done>The repo has explicit internal capability seams for dialog targets and forum topics.</done>
</task>

<task type="auto">
  <name>Task 2: Add seam-focused tests and preserve brownfield behavior</name>
  <files>tests/test_capabilities.py, tests/test_tools.py</files>
  <action>
Add `tests/test_capabilities.py` and extend `tests/test_tools.py` only where needed so the new
internal seams are test-visible independently of public tool names.

Required coverage:
- dialog-target capability coverage for resolved, not-found, and ambiguous outcomes
- forum-topic capability coverage for active topics, deleted topics, inaccessible topics, and
  stale-anchor refresh behavior
- one guard proving existing public tool schemas and brownfield action-text behavior remain stable
  after the extraction

Prefer direct tests of the new internal module where that gives a cleaner seam contract than more
tool-name-shaped assertions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_capabilities.py -q && uv run pytest tests/test_tools.py -k "list_topics or list_messages or search_messages" -q</automated>
  </verify>
  <done>The new capability seams are anchored in tests without changing current public tool behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild and restart the runtime to prove the foundation plan is live</name>
  <files>src/mcp_telegram/capabilities.py</files>
  <action>
Because this plan adds runtime code under `src/`, update the long-lived runtime on this machine
instead of relying on repo tests alone.

Required proof:
- rebuild and restart `mcp-telegram` using the documented compose file
- verify inside the container that `mcp_telegram.capabilities` imports successfully
- confirm the runtime still exposes the expected public tool names after the restart

Keep the verification lightweight. This plan is about freshness and safe startup, not public
contract change.
  </action>
  <verify>
    <automated>docker compose -f /opt/docker/mcp-telegram/docker-compose.yml up -d --build mcp-telegram && docker exec mcp-telegram /opt/venv/bin/python -c "import mcp_telegram.capabilities, mcp_telegram.tools as t; print(hasattr(t, 'ListTopics'), hasattr(t, 'ListMessages'), hasattr(t, 'SearchMessages'))"</automated>
  </verify>
  <done>The restarted runtime is confirmed to include the new capability foundation safely.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. `src/mcp_telegram/capabilities.py` exists and exposes explicit internal seams
2. `tests/test_capabilities.py` exists and targets capability behavior independent of public tool names
3. public tool schemas still reflect the same Phase 15 surface
4. topic deletion, inaccessibility, and stale-anchor recovery remain preserved
5. the restarted runtime imports the new capability module and still exposes the expected tools
6. the repo remains ready for adapter-thinning work in later plans
</verification>

<success_criteria>
- [ ] `CAP-01` has an explicit internal foundation instead of only tool-local helper sprawl
- [ ] Dialog-target and forum-topic behavior can be tested independently of public tool names
- [ ] No Phase 16 navigation or Phase 17 workflow scope enters this foundation plan
- [ ] Current brownfield tool behavior remains stable after the extraction
- [ ] The restarted runtime is refreshed and verified after the source change
- [ ] The phase has a concrete seam contract for downstream adapter migration
</success_criteria>

<output>
After completion, create `.planning/phases/15-capability-seams/15-01-SUMMARY.md` with:
- capability primitives introduced
- internal test seams added
- preserved public-surface guarantees
- quick-test command used
</output>
