---
phase: 14-boundary-recovery
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/mcp_telegram/server.py
autonomous: true
requirements:
  - ERR-01

must_haves:
  truths:
    - "Escaped tool failures no longer collapse to only `Tool <name> failed` at the server boundary"
    - "Returned error text names the tool, distinguishes the failure stage, and gives a safe next step without dumping stack traces"
    - "Existing telemetry-on-error and local action-text recovery behaviors remain intact"
  artifacts:
    - path: "src/mcp_telegram/server.py"
      provides: "Safe server-boundary recovery shaping for escaped tool failures"
      exports: ["Boundary failure formatter", "Stage-aware call_tool handling", "Runtime verification hooks"]
  key_links:
    - from: "tests/test_server.py"
      to: "src/mcp_telegram/server.py"
      via: "implements the test-anchored contract for escaped validation and runtime failures"
      pattern: "validation|runtime|actionable|Tool <name> failed"
    - from: "14-RESEARCH.md"
      to: "src/mcp_telegram/server.py"
      via: "keeps the fix bounded to the isolated boundary collapse instead of introducing a broad new error framework"
      pattern: "server.py|safe escaped-error shape|no stack traces|runtime proof"
    - from: "tests/test_tools.py"
      to: "src/mcp_telegram/server.py"
      via: "preserves telemetry-on-error and existing handler-local recovery expectations while the boundary changes"
      pattern: "tool_records_telemetry_on_error|get_user_info_fetch_error_returns_action|list_messages_invalid_cursor_returns_error"
---

<objective>
Implement the Phase 14 boundary recovery behavior and prove it in the repo and live runtime.

Purpose: replace the generic escaped-error wrapper with a safe, actionable boundary surface.
Output: `src/mcp_telegram/server.py` updated to preserve actionable recovery direction for escaped
failures, plus repository and restarted-runtime proof.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-boundary-recovery/14-RESEARCH.md
@.planning/phases/14-boundary-recovery/14-VALIDATION.md
@.planning/phases/14-boundary-recovery/14-01-PLAN.md
@src/mcp_telegram/server.py
@src/mcp_telegram/tools.py
@tests/test_server.py
@tests/test_tools.py
@/home/j2h4u/repos/j2h4u/mcp-telegram/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a safe escaped-error formatter at the server boundary</name>
  <files>src/mcp_telegram/server.py</files>
  <action>
Update `server.call_tool()` so escaped failures no longer become only `RuntimeError("Tool <name> failed")`.

Required behavior:
- distinguish tool-argument validation/construction failures from runtime execution failures
- keep the tool name visible
- surface concise safe detail and one next-step hint
- avoid raw stack traces or sensitive payload exposure in returned error text
- keep the implementation bounded; prefer one small helper in `server.py` over a new cross-repo
  exception framework unless the tests prove that is insufficient
  </action>
  <verify>
    <automated>uv run pytest tests/test_server.py -q</automated>
  </verify>
  <done>The server boundary preserves actionable recovery direction for escaped failures.</done>
</task>

<task type="auto">
  <name>Task 2: Prove telemetry safety and restarted-runtime behavior</name>
  <files>src/mcp_telegram/server.py</files>
  <action>
Run the existing brownfield regression anchors and then verify the changed runtime on this machine.

Required proof:
- `tests/test_tools.py` still shows telemetry recording on error and existing action-text recovery
- the live `mcp-telegram` container is rebuilt and restarted after the code change
- one reproducible escaped failure through the restarted runtime now returns actionable detail
  instead of only the generic boundary wrapper
- logs remain diagnostic without widening into message-content or identifying-payload leakage
  </action>
  <verify>
    <automated>uv run pytest tests/test_tools.py -k "tool_records_telemetry_on_error or get_user_info_fetch_error_returns_action or list_messages_invalid_cursor_returns_error" -q && uv run pytest tests/test_server.py -q</automated>
  </verify>
  <done>The boundary change is proven against repo regressions and the restarted runtime.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. `src/mcp_telegram/server.py` no longer raises only the generic boundary wrapper for escaped failures
2. Returned error text identifies the tool and failure stage with actionable guidance
3. `tests/test_server.py` passes
4. Existing telemetry and handler-recovery regression anchors still pass
5. The rebuilt and restarted runtime exhibits the same improved failure behavior
</verification>

<success_criteria>
- [ ] `ERR-01` is satisfied through actionable escaped-error detail at the real server boundary
- [ ] The implementation stays bounded to the boundary problem and does not introduce speculative architecture
- [ ] Brownfield tests make the new boundary behavior explicit
- [ ] Telemetry remains privacy-safe and still records error categories on unexpected failures
- [ ] Runtime verification proves the live boundary behavior matches the repository contract
</success_criteria>

<output>
After completion, create `.planning/phases/14-boundary-recovery/14-02-SUMMARY.md` with:
- boundary formatter behavior
- failure stages covered
- telemetry/runtime proof
- restart verification result
</output>
