---
phase: 14-boundary-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_server.py
autonomous: true
requirements:
  - ERR-01

must_haves:
  truths:
    - "Phase 14 makes the escaped-error boundary explicit before implementation changes land"
    - "Validation-stage failures and runtime-stage failures are anchored as distinct boundary cases"
    - "Existing handler-local action text remains outside the escaped-error wrapper because it does not raise"
  artifacts:
    - path: "tests/test_server.py"
      provides: "Brownfield contract tests for actionable server-boundary recovery"
      exports: ["Validation-stage failure contract", "Escaped runtime failure contract", "Pass-through recovery contract"]
  key_links:
    - from: "14-RESEARCH.md"
      to: "tests/test_server.py"
      via: "turns the isolated server-boundary collapse into an executable test contract"
      pattern: "server.call_tool|safe escaped-error shape|validation-stage|runtime-stage"
    - from: "src/mcp_telegram/server.py"
      to: "tests/test_server.py"
      via: "anchors the real brownfield collapse point instead of testing a fixture-only fake server"
      pattern: "tools.tool_args|tools.tool_runner|RuntimeError|Tool <name> failed"
    - from: "tests/test_tools.py"
      to: "tests/test_server.py"
      via: "preserves the distinction between local action-text recovery and escaped boundary failures"
      pattern: "get_user_info_fetch_error_returns_action|list_messages_invalid_cursor_returns_error|tool_records_telemetry_on_error"
---

<objective>
Create the brownfield contract tests for Phase 14.

Purpose: define the intended server-boundary failure behavior before the boundary implementation is
changed.
Output: `tests/test_server.py` with direct coverage of `server.call_tool()` escaped-error shaping.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-boundary-recovery/14-RESEARCH.md
@.planning/phases/14-boundary-recovery/14-VALIDATION.md
@.planning/phases/13-implementation-sequencing-decision-memo/13-IMPLEMENTATION-MEMO.md
@src/mcp_telegram/server.py
@src/mcp_telegram/tools.py
@tests/test_tools.py
@tests/test_mcp_test_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add boundary contract tests for escaped validation and runtime failures</name>
  <files>tests/test_server.py</files>
  <action>
Create `tests/test_server.py` and add focused tests around `server.call_tool()`.

Required coverage:
- a tool-argument construction failure keeps the tool name visible and reports that validation or
  argument parsing failed
- an unexpected exception from tool execution keeps the tool name visible and reports that runtime
  execution failed
- each failure shape includes actionable next-step guidance rather than only `Tool <name> failed`

Use monkeypatching around `tools.tool_args(...)` and/or `tools.tool_runner(...)` so the tests stay
fast and isolate the real boundary.
  </action>
  <verify>
    <automated>uv run pytest tests/test_server.py -k "validation or escaped" -q</automated>
  </verify>
  <done>The server boundary contract is explicit for the two escaped failure stages Phase 14 must fix.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the pass-through boundary behavior for existing action-text recovery</name>
  <files>tests/test_server.py</files>
  <action>
Extend `tests/test_server.py` with one contract test that proves a handler returning actionable
`TextContent` is passed through unchanged and does not get rewrapped as an escaped boundary error.

Keep this test narrow. It exists to protect the existing brownfield recovery strengths while the
boundary wrapper changes.

Also add one control-case assertion, if it keeps the file simple, that `unknown tool` or non-dict
`arguments` still fail directly outside the escaped-error contract. The goal is to keep Phase 14
bounded to ERR-01 instead of silently expanding it into a full boundary-taxonomy rewrite.
  </action>
  <verify>
    <automated>uv run pytest tests/test_server.py -k "passthrough or contract" -q</automated>
  </verify>
  <done>The test contract distinguishes escaped failures from already-handled recovery text.</done>
</task>

</tasks>

<verification>
After plan completion, verify:
1. `tests/test_server.py` exists
2. The file covers escaped validation-stage failure and escaped runtime-stage failure separately
3. At least one assertion proves the returned failure text is actionable rather than generic
4. At least one assertion proves action-text recovery still passes through unchanged
5. The new tests are fast enough for the quick validation loop
</verification>

<success_criteria>
- [ ] `tests/test_server.py` exists with dedicated Phase 14 boundary contract tests
- [ ] `ERR-01` is anchored through distinct validation-stage and runtime-stage failure checks
- [ ] The tests fail if the boundary collapses to generic `Tool <name> failed`
- [ ] Existing handler-local recovery remains explicitly protected
- [ ] The phase has a stable brownfield test anchor for later Medium contract changes
</success_criteria>

<output>
After completion, create `.planning/phases/14-boundary-recovery/14-01-SUMMARY.md` with:
- covered boundary cases
- escaped failure stages
- pass-through recovery safeguard
- quick-test command used
</output>
