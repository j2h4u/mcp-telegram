# Archived dotMD adapter

dotMD integration is archived and stopped. This source-export snapshot is
non-runtime reference material only; it is not an active downstream contract.
Git history is the canonical record for its origin and evolution.
